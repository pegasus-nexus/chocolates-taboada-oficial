---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import CallToAction from "../components/CallToAction.astro";
import Testimonials from "../components/Testimonials.astro";
import Footer from "../components/Footer.astro";
import Products from "../components/Products.astro";
import MobileCTA from "../components/MobileCTA.astro";
import PromoPopup from "../components/PromoPopup.astro";
---

<Layout
  title="El sabor de la tradición | Chocolates Taboada"
  description="Descubre Chocolates Taboada, la primera industria chocolatera de Bolivia desde 1948. Tradición, elegancia y un sabor inolvidable."
>
  <PromoPopup />
  <Navbar />

  <header
    id="inicio"
    class="relative min-h-screen overflow-hidden bg-[#14080d]"
  >
    <div
      id="hero-bg-cover"
      class="absolute inset-0 bg-no-repeat
           bg-cover bg-[position:80%_center] scale-100
           lg:bg-center lg:scale-105 transition-all duration-700"
      data-bg-cochabamba="/img/portadataboada.png"
      data-bg-lapaz="/img/agostolp.png"
      style="background-image:url('/img/portadataboada.png')"
    >
    </div>

    <div
      class="absolute inset-0 bg-[linear-gradient(to_right,rgba(10,5,7,0.96)_0%,rgba(20,10,15,0.92)_20%,rgba(38,18,25,0.82)_42%,rgba(62,28,40,0.58)_68%,rgba(120,70,85,0.18)_88%,rgba(255,255,255,0)_100%)]"
    >
    </div>

    <!-- BRILLO PREMIUM -->

    <div
      class="absolute top-[-180px] left-[-120px] w-[500px] h-[500px] rounded-full bg-[#f6d98d]/10 blur-[120px]"
    >
    </div>

    <!-- PARTICULAS -->

    <div class="absolute inset-0 overflow-hidden z-[2]">
      <!-- LUCES -->

      <span
        class="absolute top-[52%] left-[20%] w-2 h-2 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float"
      ></span>

      <span
        class="absolute top-[48%] left-[34%] w-1 h-1 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:1s]"
      ></span>

      <span
        class="absolute top-[58%] left-[48%] w-2 h-2 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:2s]"
      ></span>

      <span
        class="absolute top-[66%] left-[64%] w-1.5 h-1.5 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:3s]"
      ></span>

      <!-- PETALOS (SVG inline) -->

      <!-- CHOCOLATE Y CACAO FALL (SVGs) -->
      <svg
        class="chocolate-fall absolute left-[5%] w-3 opacity-60"
        style="animation-duration:9s; animation-delay:2.5s;"
        viewBox="0 0 10 10"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><circle cx="5" cy="5" r="4" fill="#3D1C06"></circle></svg
      >
      <svg
        class="chocolate-fall absolute left-[12%] w-7 opacity-75"
        style="animation-duration:14s; animation-delay:0s;"
        viewBox="0 0 40 60"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path d="M20,10 C32,20 32,40 20,50 C8,40 8,20 20,10 Z" fill="#3D1C06"
        ></path><path
          d="M20,15 C24,22 24,38 20,45"
          stroke="#D4AF37"
          stroke-width="1.5"
          stroke-linecap="round"
          opacity="0.6"></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[22%] w-5 opacity-60"
        style="animation-duration:9s; animation-delay:1s;"
        viewBox="0 0 30 30"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path
          d="M15,5 L17,12 L24,15 L17,18 L15,25 L13,18 L6,15 L13,12 Z"
          fill="#2A1612"></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[33%] w-5 opacity-65"
        style="animation-duration:13s; animation-delay:6s;"
        viewBox="0 0 40 60"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path d="M20,15 C28,23 28,37 20,45 C12,37 12,23 20,15 Z" fill="#42200D"
        ></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[45%] w-8 opacity-80"
        style="animation-duration:11s; animation-delay:3s;"
        viewBox="0 0 40 60"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path d="M20,12 C30,22 30,38 20,48 C10,38 10,22 20,12 Z" fill="#5C3821"
        ></path><path
          d="M20,17 C23,24 23,36 20,43"
          stroke="#F6D98D"
          stroke-width="1.2"
          stroke-linecap="round"
          opacity="0.5"></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[53%] w-4 opacity-60"
        style="animation-duration:12s; animation-delay:5s;"
        viewBox="0 0 20 20"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><polygon points="10,3 17,7 15,15 7,17 3,10" fill="#2E160A"
        ></polygon></svg
      >
      <svg
        class="chocolate-fall absolute left-[62%] w-4 opacity-75"
        style="animation-duration:8s; animation-delay:4s;"
        viewBox="0 0 30 30"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path
          d="M15,7 L17,12 L22,15 L17,18 L15,23 L13,18 L8,15 L13,12 Z"
          fill="#D4AF37"></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[70%] w-3 opacity-80"
        style="animation-duration:7s; animation-delay:0.5s;"
        viewBox="0 0 10 10"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><circle cx="5" cy="5" r="4" fill="#D4AF37"></circle></svg
      >
      <svg
        class="chocolate-fall absolute left-[78%] w-6 opacity-70"
        style="animation-duration:16s; animation-delay:1.5s;"
        viewBox="0 0 40 60"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path
          d="M20,10 C32,20 32,40 20,50 C8,40 8,20 20,10 Z"
          fill="url(#goldGrad)"></path><defs
          ><linearGradient id="goldGrad" x1="0%" y1="0%" x2="100%" y2="100%"
            ><stop offset="0%" stop-color="#D4AF37"></stop><stop
              offset="50%"
              stop-color="#F6D98D"></stop><stop
              offset="100%"
              stop-color="#AA7C11"></stop></linearGradient
          ></defs
        ></svg
      >
      <svg
        class="chocolate-fall absolute left-[88%] w-5 opacity-70"
        style="animation-duration:10s; animation-delay:2s;"
        viewBox="0 0 30 30"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><path
          d="M15,5 L17,12 L24,15 L17,18 L15,25 L13,18 L6,15 L13,12 Z"
          fill="#7A4B29"></path></svg
      >
      <svg
        class="chocolate-fall absolute left-[93%] w-2.5 opacity-75"
        style="animation-duration:8s; animation-delay:7s;"
        viewBox="0 0 10 10"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        ><circle cx="5" cy="5" r="4" fill="#F6D98D"></circle></svg
      >
    </div>

    <!-- CONTENIDO -->

    <div
      class="relative z-[5] w-full max-w-[1440px] mx-auto min-h-screen px-6 lg:pl-16 lg:pr-10 pt-[120px] pb-20 grid lg:grid-cols-2 items-center gap-8"
    >
      <!-- TEXTO -->

      <div class="max-w-[650px]">
        <!-- MINI TEXTO -->

        <div class="flex items-center gap-4 mb-6">
          <div
            class="w-10 h-[1px] bg-gradient-to-r from-[#dcb041] to-transparent"
          >
          </div>

          <span
            id="hero-subtitle"
            class="uppercase tracking-[0.38em] text-[#dcb041] text-[0.75rem] font-bold"
          >
            DESDE 1948 CREANDO MOMENTOS ESPECIALES
          </span>
        </div>

        <!-- TITULO -->

        <h1
          id="hero-title"
          class="font-playfair text-[clamp(2.2rem,4.8vw,4.2rem)] leading-[1.15] tracking-[-0.03em] text-[#fff7f8] drop-shadow-[0_12px_35px_rgba(0,0,0,0.45)]"
        >
          <span class="whitespace-nowrap block">Hay momentos que</span>
          <span class="text-[#dcb041] italic font-[600] block mt-1"
            >merecen un buen chocolate.</span
          >
        </h1>

        <!-- TEXTO -->

        <p
          id="hero-description"
          class="mt-6 text-[clamp(0.95rem,1.5vw,1.1rem)] leading-[1.8] text-[#f3dfe5]/75 max-w-[460px] font-light"
        >
          Cada ocasión merece un detalle especial. Descubre chocolates
          elaborados con tradición, elegancia y el auténtico sabor boliviano
          para regalar, compartir o simplemente darte un gusto.
        </p>

        <!-- BOTONES -->

        <div class="mt-10 flex flex-wrap items-center gap-3.5">
          <a
            href="#cajas-especiales"
            class="group inline-flex items-center gap-2.5 px-7 py-4 rounded-full bg-[linear-gradient(145deg,#f6d98d,#dcb041)] text-[#2e1119] font-bold text-xs md:text-sm shadow-[0_10px_25px_rgba(220,176,65,0.2)] hover:scale-[1.03] transition-all duration-500 relative overflow-hidden"
          >
            <span class="relative z-10 tracking-wide">Explorar Colecciones</span
            >
            <div
              class="absolute inset-0 bg-gradient-to-r from-transparent via-white/60 to-transparent -translate-x-[130%] group-hover:animate-shimmer"
            >
            </div>
          </a>

          <a
            href="/nosotros"
            class="group inline-flex items-center gap-2.5 px-7 py-4 text-warm-white hover:text-[#dcb041] font-bold text-xs md:text-sm transition-all duration-500 relative overflow-hidden"
          >
            <span class="relative z-10 tracking-wide">Nuestra tradición</span>
          </a>
        </div>
      </div>
    </div>
  </header>

  <script>
    document.addEventListener('DOMContentLoaded', async () => {
      try {
        const apiUrl = import.meta.env.PUBLIC_API_URL || "http://localhost:10000/api/v1";
        const response = await fetch(`${apiUrl}/fidelizacion/catalog`);
        if (response.ok) {
          const data = await response.json();
          if (data.status === 'success' && data.web_config) {
            const wc = data.web_config;
            if (wc.hero_subtitle) document.getElementById('hero-subtitle').textContent = wc.hero_subtitle;
            if (wc.hero_title) document.getElementById('hero-title').textContent = wc.hero_title;
            if (wc.hero_description) document.getElementById('hero-description').textContent = wc.hero_description;
            
            const bgElement = document.getElementById('hero-bg-cover');
            if (bgElement) {
              if (wc.hero_bg_cba) bgElement.setAttribute('data-bg-cochabamba', wc.hero_bg_cba);
              if (wc.hero_bg_lpz) bgElement.setAttribute('data-bg-lapaz', wc.hero_bg_lpz);
              
              const currentCity = localStorage.getItem('taboada_selected_city') || 'cochabamba';
              window.dispatchEvent(new CustomEvent('cityChanged', { detail: { city: currentCity } }));
            }
          }
        }
      } catch (err) {
        console.error("Error loading web config:", err);
      }
    });
  </script>

  <section
    class="relative py-20 md:py-24 bg-[#FAF5F0] overflow-hidden border-y border-[#f0e6d8]"
  >
    <!-- Sutil brillo decorativo de fondo -->
    <div
      class="absolute -right-20 top-1/2 -translate-y-1/2 w-48 h-48 bg-gradient-radial from-amber-200/10 to-transparent blur-3xl pointer-events-none"
    >
    </div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      <!-- CABECERA SUAVE: Centrada y con fondo aclarado -->
      <div class="w-full max-w-4xl mx-auto mb-12 p-8 rounded-2xl text-center">
        <!-- Categoría Centrada -->
        <div class="flex items-center justify-center gap-2 mb-4">
          <span class="w-6 h-[2px] bg-[#8f6c49]"></span>
          <span
            class="text-xs uppercase tracking-widest text-[#8f6c49] font-bold font-sans"
            >¿Por qué comprar en Taboada?</span
          >
          <span class="w-6 h-[2px] bg-[#8f6c49]"></span>
        </div>

        <!-- Título Principal Centrado -->
        <h2
          class="font-serif text-3xl md:text-5xl text-[#2A1612] font-extrabold leading-tight tracking-tight mb-4"
        >
          Calidad, variedad y facilidad de compra.
        </h2>

        <!-- Mini Texto Agregado (Ajustado para Celular) -->
        <p
          class="text-sm md:text-base text-[#5D4037]/80 max-w-lg mx-auto leading-relaxed"
        >
          Encuentra chocolates elaborados con ingredientes seleccionados, una
          amplia variedad de opciones para cada ocasión y un proceso de compra
          rápido, sencillo
        </p>
      </div>

      <!-- GRILLA DE BENEFICIOS / CARACTERÍSTICAS -->
      <div
        class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 items-stretch"
      >
        <!-- Tarjeta 1: Regalar -->
        <a
          href="#cajas-especiales"
          class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10"
        >
          <!-- Imagen de fondo texturada -->
          <img
            src="/img/sugerencia1.png"
            alt="Calidad Inigualable"
            class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 pointer-events-none"
          />
          <!-- Overlay degradado oscuro gradual -->
          <div
            class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"
          >
          </div>
          <!-- Overlay café en hover -->
          <div
            class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"
          >
          </div>

          <!-- Contenido en la base -->
          <div
            class="relative z-10 flex flex-col items-start text-left text-white"
          >
            <!-- Icono blanco -->
            <div
              class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500"
            >
              <svg
                class="w-6 h-6"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <circle cx="12" cy="8" r="7"></circle>
                <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"
                ></polyline>
              </svg>
            </div>

            <h3
              class="font-serif font-bold text-lg md:text-2xl text-white mb-2 leading-tight"
            >
              Regalar
            </h3>

            <!-- Descripción que se despliega elegantemente en hover -->
            <div
              class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out"
            >
              <p
                class="overflow-hidden text-[11px] md:text-lg text-white/80 leading-relaxed font-sans transition-all duration-500"
              >
                Encuentra cajas, detalles y obsequios para sorprender en cualquier ocasión.
              </p>
            </div>
          </div>
        </a>

        <!-- Tarjeta 2: Sorprender -->
        <a
          href="#cajas-especiales"
          class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10"
        >
          <!-- Imagen de fondo texturada -->
          <img
            src="/img/sugerencia2.jpeg"
            alt="Maestría y Tradición"
            class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 pointer-events-none"
          />
          <!-- Overlay degradado oscuro gradual -->
          <div
            class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"
          >
          </div>
          <!-- Overlay café en hover -->
          <div
            class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"
          >
          </div>

          <!-- Contenido en la base -->
          <div
            class="relative z-10 flex flex-col items-start text-left text-white"
          >
            <!-- Icono blanco -->
            <div
              class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500"
            >
              <svg
                class="w-6 h-6"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path
                  d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"
                ></path>
                <polyline points="14 2 14 8 20 8"></polyline>
                <line x1="16" y1="13" x2="8" y2="13"></line>
                <line x1="16" y1="17" x2="8" y2="17"></line>
              </svg>
            </div>

            <h3
              class="font-serif font-bold text-lg md:text-2xl text-white mb-2 leading-tight"
            >
              Sorprender
            </h3>

            <!-- Descripción que se despliega elegantemente en hover -->
            <div
              class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out"
            >
              <p
                class="overflow-hidden text-[11px] md:text-lg text-white/80 leading-relaxed font-sans transition-all duration-500"
              >
                Colecciones pensadas para aniversarios, cumpleaños y momentos especiales.
              </p>
            </div>
          </div>
        </a>

        <!-- Tarjeta 3: Disfrutar -->
        <a
          href="#cajas-especiales"
          class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10"
        >

          <img
            src="/img/sugerencia3.png"
            alt="Ingredientes de Origen Noble"
            class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 pointer-events-none"
          />
          <!-- Overlay degradado oscuro gradual -->
          <div
            class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"
          >
          </div>
          <!-- Overlay café en hover -->
          <div
            class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"
          >
          </div>

          <!-- Contenido en la base -->
          <div
            class="relative z-10 flex flex-col items-start text-left text-white"
          >
            <!-- Icono blanco -->
            <div
              class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500"
            >
              <svg
                class="w-6 h-6"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path
                  d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 3.58 1 9.3a7 7 0 0 1-9 8.7z"
                ></path>
                <path d="M9 22v-4"></path>
              </svg>
            </div>

            <h3
              class="font-serif font-bold text-lg md:text-2xl text-white mb-2 leading-tight"
            >
              Disfrutar
            </h3>

            <!-- Descripción que se despliega elegantemente en hover -->
            <div
              class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out"
            >
              <p
                class="overflow-hidden text-[11px] md:text-lg text-white/80 leading-relaxed font-sans transition-all duration-500"
              >
                Bombones, tabletas, café y chocolates para darte un gusto todos los días.
              </p>
            </div>
          </div>
        </a>

        <!-- Tarjeta 4: Compartir -->
        <a
          href="#cajas-especiales"
          class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10"
        >
          <!-- Imagen de fondo texturada -->
          <img
            src="/img/sugerencia4.png"
            alt="Sofisticación en Cada Detalle"
            class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 pointer-events-none"
          />
          <!-- Overlay degradado oscuro gradual -->
          <div
            class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"
          >
          </div>
          <!-- Overlay café en hover -->
          <div
            class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"
          >
          </div>

          <!-- Contenido en la base -->
          <div
            class="relative z-10 flex flex-col items-start text-left text-white"
          >
            <!-- Icono blanco -->
            <div
              class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500"
            >
              <svg
                class="w-6 h-6"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M6 3h12l4 6-10 13L2 9z"></path>
                <path d="M11 3 8 9l4 13 4-13-3-6"></path>
                <path d="M2 9h20"></path>
              </svg>
            </div>

            <h3
              class="font-serif font-bold text-lg md:text-2xl text-white mb-2 leading-tight"
            >
              Compartir
            </h3>

            <!-- Descripción que se despliega elegantemente en hover -->
            <div
              class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out"
            >
              <p
                class="overflow-hidden text-[11px] md:text-lg text-white/80 leading-relaxed font-sans transition-all duration-500"
              >
                Packs y presentaciones ideales para reuniones, familia y amigos.
              </p>
            </div>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- SECCIÓN PROMOCIONES ESPECIALES (CON FONDO3.PNG Y CAPA OSCURA MÁS INTENSA DE SÚPER LUJO) -->
  <section
    id="elegancia"
    class="relative py-20 md:py-28 overflow-hidden bg-[#180a0e] text-[#FAF5F0]"
  >
    <!-- Fondo fondo3.png con destellos y pétalos -->
    <div
      class="absolute inset-0 bg-cover bg-center pointer-events-none opacity-90"
      style="background-image: url('/img/fondo3.png');"
    >
    </div>

    <!-- Capa sutil de oscurecimiento equilibrado -->
    <div
      class="absolute inset-0 bg-gradient-to-b from-[#180a0e]/40 via-[#120609]/25 to-[#180a0e]/50 pointer-events-none"
    >
    </div>

    <!-- ESTRUCTURA Y LLUVIA PATRIA AMPLIADA (PUNTOS TRICOLORES GRANDES, BANDERITAS Y KANTUTAS) -->
    <style is:inline>
      @keyframes patrioticFall {
        0% {
          transform: translateY(-50px) rotate(0deg) scale(0.85);
          opacity: 0;
        }
        15% {
          opacity: 0.95;
        }
        85% {
          opacity: 0.9;
        }
        100% {
          transform: translateY(850px) rotate(360deg) scale(1.35);
          opacity: 0;
        }
      }

      .patriotic-particle {
        position: absolute;
        top: -60px;
        pointer-events: none;
        animation: patrioticFall linear infinite;
        z-index: 5;
      }
    </style>

    <!-- ANIMACIÓN DE PARTÍCULAS TRICOLORES Y KANTUTAS EN EL FONDO (TAMAÑO AMPLIO) -->
    <div class="absolute inset-0 pointer-events-none overflow-hidden z-5">
      <!-- PUNTOS TRICOLORES GLOWING GRANDES -->
      <div class="patriotic-particle" style="left: 4%; animation-duration: 7.5s; animation-delay: 0s;">
        <div class="w-6 h-6 rounded-full bg-[#D8232A] shadow-[0_0_20px_#D8232A]"></div>
      </div>
      <div class="patriotic-particle" style="left: 13%; animation-duration: 9.5s; animation-delay: 1.5s;">
        <div class="w-5 h-5 rounded-full bg-[#FCE300] shadow-[0_0_18px_#FCE300]"></div>
      </div>
      <div class="patriotic-particle" style="left: 22%; animation-duration: 8.2s; animation-delay: 3.2s;">
        <div class="w-6 h-6 rounded-full bg-[#008751] shadow-[0_0_20px_#008751]"></div>
      </div>

      <!-- KANTUTA FLOWER SVG 1 (GRANDE) -->
      <div class="patriotic-particle" style="left: 28%; animation-duration: 10.5s; animation-delay: 0.5s;">
        <svg class="w-10 h-10 md:w-12 md:h-12 filter drop-shadow-[0_4px_12px_rgba(0,0,0,0.8)] opacity-95" viewBox="0 0 24 24" fill="none">
          <path d="M12 2C9 5 7 8 7 12c0 2.5 1 4 2 5.5v2.5a1 1 0 001.6.8l1.4-1.05 1.4 1.05A1 1 0 0015 20v-2.5c1-1.5 2-3 2-5.5 0-4-2-7-5-10z" fill="#D8232A"/>
          <path d="M9.5 11c1.5 1 3.5 1 5 0" stroke="#FCE300" stroke-width="2" stroke-linecap="round"/>
          <circle cx="12" cy="18.5" r="2" fill="#008751"/>
        </svg>
      </div>

      <!-- BANDERITA TRICOLOR 1 (GRANDE) -->
      <div class="patriotic-particle" style="left: 37%; animation-duration: 8.8s; animation-delay: 2.1s;">
        <div class="flex flex-col w-6 h-8 md:w-7 md:h-9 rounded-md overflow-hidden shadow-xl border border-white/30 opacity-95 rotate-12">
          <div class="h-1/3 bg-[#D8232A]"></div>
          <div class="h-1/3 bg-[#FCE300]"></div>
          <div class="h-1/3 bg-[#008751]"></div>
        </div>
      </div>

      <!-- PUNTOS TRICOLORES GLOWING GRANDES -->
      <div class="patriotic-particle" style="left: 46%; animation-duration: 7.5s; animation-delay: 4.0s;">
        <div class="w-7 h-7 rounded-full bg-[#FCE300] shadow-[0_0_22px_#FCE300]"></div>
      </div>
      <div class="patriotic-particle" style="left: 55%; animation-duration: 9.2s; animation-delay: 0.8s;">
        <div class="w-5.5 h-5.5 rounded-full bg-[#008751] shadow-[0_0_18px_#008751]"></div>
      </div>

      <!-- KANTUTA FLOWER SVG 2 (GRANDE) -->
      <div class="patriotic-particle" style="left: 63%; animation-duration: 11.5s; animation-delay: 3.5s;">
        <svg class="w-10 h-10 md:w-12 md:h-12 filter drop-shadow-[0_4px_12px_rgba(0,0,0,0.8)] opacity-95" viewBox="0 0 24 24" fill="none">
          <path d="M12 2C9 5 7 8 7 12c0 2.5 1 4 2 5.5v2.5a1 1 0 001.6.8l1.4-1.05 1.4 1.05A1 1 0 0015 20v-2.5c1-1.5 2-3 2-5.5 0-4-2-7-5-10z" fill="#D8232A"/>
          <path d="M9.5 11c1.5 1 3.5 1 5 0" stroke="#FCE300" stroke-width="2" stroke-linecap="round"/>
          <circle cx="12" cy="18.5" r="2" fill="#008751"/>
        </svg>
      </div>

      <!-- BANDERITA TRICOLOR 2 (GRANDE) -->
      <div class="patriotic-particle" style="left: 72%; animation-duration: 8.5s; animation-delay: 1.2s;">
        <div class="flex flex-col w-6 h-8 md:w-7 md:h-9 rounded-md overflow-hidden shadow-xl border border-white/30 opacity-95 -rotate-12">
          <div class="h-1/3 bg-[#D8232A]"></div>
          <div class="h-1/3 bg-[#FCE300]"></div>
          <div class="h-1/3 bg-[#008751]"></div>
        </div>
      </div>

      <!-- PUNTOS TRICOLORES Y DESTELLOS DORADOS -->
      <div class="patriotic-particle" style="left: 81%; animation-duration: 8.0s; animation-delay: 2.8s;">
        <div class="w-6 h-6 rounded-full bg-[#D8232A] shadow-[0_0_20px_#D8232A]"></div>
      </div>
      <div class="patriotic-particle" style="left: 89%; animation-duration: 10s; animation-delay: 5.1s;">
        <div class="w-5 h-5 rounded-full bg-[#FCE300] shadow-[0_0_18px_#FCE300]"></div>
      </div>
      <div class="patriotic-particle" style="left: 95%; animation-duration: 8.8s; animation-delay: 0.3s;">
        <div class="w-6 h-6 rounded-full bg-[#008751] shadow-[0_0_20px_#008751]"></div>
      </div>

      <!-- DESTELLOS DORADOS SECUNDARIOS GRANDES -->
      <div class="patriotic-particle" style="left: 17%; animation-duration: 11s; animation-delay: 4.6s;">
        <div class="w-4 h-4 rounded-full bg-[#dcb041] shadow-[0_0_14px_#dcb041]"></div>
      </div>
      <div class="patriotic-particle" style="left: 51%; animation-duration: 9.4s; animation-delay: 5.8s;">
        <div class="w-4.5 h-4.5 rounded-full bg-[#dcb041] shadow-[0_0_16px_#dcb041]"></div>
      </div>
      <div class="patriotic-particle" style="left: 78%; animation-duration: 9.6s; animation-delay: 4.2s;">
        <div class="w-4 h-4 rounded-full bg-[#dcb041] shadow-[0_0_14px_#dcb041]"></div>
      </div>
    </div>

    <div class="max-w-6xl mx-auto px-5 relative z-10">
      <!-- TÍTULO DE LA SECCIÓN IGUAL AL MOCKUP DE REFERENCIA -->
      <div class="text-center mb-14">
        <h2
          class="text-3xl md:text-5xl font-serif text-white mb-2 tracking-tight font-bold drop-shadow-md"
        >
          Destacados de la Temporada
        </h2>

        <p
          class="font-cursiva text-xl md:text-3xl text-[#dcb041] tracking-wide drop-shadow-[0_0_12px_rgba(220,176,65,0.35)]"
        >
          Descubre las colecciones y productos más relevantes del momento,
          seleccionados para la ocasión.
        </p>
      </div>

      <!-- TARJETAS GLASSMORPHIC MÁS OSCURAS Y SOFISTICADAS -->
      <div class="flex flex-col gap-8 max-w-5xl mx-auto">
        <!-- CARD 1: Caja Madre Rosada / Estuche Selección Premium -->
        <div
          class="product-card bg-[#18090d]/35 backdrop-blur-md border border-white/10 rounded-[2rem] p-6 md:p-8 flex flex-col md:flex-row items-center gap-8 shadow-2xl transition-all duration-500 group cursor-pointer"
          style="-webkit-backdrop-filter: blur(12px);"
          data-id="tableta-amaranto"
          data-name="TABLETA DE CHOCOLATE CON AMARANTO"
          data-size="100g"
          data-img="/img/especial 3.png"
          data-desc="Más que chocolate... una forma elegante de decir gracias."
          data-price-cochabamba="BS. 29.5"
          data-price-lapaz="BS. 29.5"
        >
          <!-- IMAGEN -->
          <div
            class="md:w-[42%] flex items-center justify-center relative overflow-hidden"
          >
            <img
              src="/img/especial 3.png"
              alt="Caja Madre Rosada"
              class="object-contain w-full h-full max-h-56 relative z-10 drop-shadow-[0_15px_25px_rgba(0,0,0,0.6)] group-hover:scale-105 transition-transform duration-500"
            />
          </div>

          <!-- CONTENIDO Y TEXTOS -->
          <div class="md:w-[58%] flex flex-col justify-center text-left md:pl-4 lg:pl-6">
            <h3
              class="text-4xl md:text-5xl font-serif font-bold text-white mb-2 drop-shadow-sm"
            >
              Bs. 29.5
            </h3>

            <h4
              class="text-xl md:text-2xl font-bold text-[#dcb041] mb-1 font-serif tracking-wide"
            >
              TABLETA DE CHOCOLATE CON AMARANTO

              <span class="font-normal text-white/70 text-sm font-sans ml-1">
                100g
              </span>
            </h4>

            <p class="text-[#f6d98d]/90 italic text-sm mb-2 font-serif">
              "Eres una mamá increíble"
            </p>

            <p class="text-white/80 text-sm leading-relaxed mb-6 font-sans">
              Más que chocolate... una forma elegante de decir "gracias".
            </p>

            <div class="flex flex-wrap items-center gap-3 mt-auto">
              <span
                class="bg-black/40 border border-white/15 text-white/80 text-xs font-semibold px-4 py-2 rounded-full font-sans tracking-wide"
              >
                Edición Especial
              </span>

              <!-- Botón Añadir al Carrito con Pop-up de Cantidad -->
              <button
                type="button"
                class="add-to-cart-btn bg-[#dcb041] hover:bg-white text-[#1C0C12] text-xs font-extrabold px-5 py-2.5 rounded-full transition-all duration-300 shadow-lg hover:scale-105 cursor-pointer flex items-center gap-2"
                data-id="tableta-amaranto"
                data-name="TABLETA DE CHOCOLATE CON AMARANTO"
                data-size="100g"
                data-img="/img/especial 3.png"
                data-desc="Más que chocolate... una forma elegante de decir gracias."
                data-price-cochabamba="BS. 29.5"
                data-price-lapaz="BS. 29.5"
              >
                <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" /></svg>
                <span>Añadir al Carrito</span>
              </button>

              <!-- Comprar directamente por WhatsApp -->
              <button
                type="button"
                class="direct-buy-btn bg-black/50 hover:bg-black/70 border border-white/20 text-white text-xs font-bold px-5 py-2.5 rounded-full transition-all duration-300 shadow-md hover:scale-105 text-center cursor-pointer flex items-center gap-2"
                data-name="TABLETA DE CHOCOLATE CON AMARANTO"
                data-size="100g"
                data-price-cochabamba="BS. 29.5"
                data-price-lapaz="BS. 29.5"
              >
                Comprar <span class="text-sm">→</span>
              </button>
            </div>
          </div>
        </div>

        <!-- CARD 2: Caja Cartera -->
        <div
          class="product-card bg-[#18090d]/35 backdrop-blur-md border border-white/10 rounded-[2rem] p-6 md:p-8 flex flex-col md:flex-row-reverse items-center gap-8 shadow-2xl transition-all duration-500 group cursor-pointer"
          style="-webkit-backdrop-filter: blur(12px);"
          data-id="tableta-quinua"
          data-name="TABLETA DE CHOCOLATE CON QUINUA"
          data-size="100g"
          data-img="/img/Especial 4.png"
          data-desc="Un detalle delicado, elegante y lleno de cariño."
          data-price-cochabamba="BS. 29.5"
          data-price-lapaz="BS. 29.5"
        >
          <!-- IMAGEN -->
          <div
            class="md:w-[42%] flex items-center justify-center relative overflow-hidden"
          >
            <img
              src="/img/Especial 4.png"
              alt="Caja Cartera"
              class="object-contain w-full h-full max-h-56 relative z-10 drop-shadow-[0_15px_25px_rgba(0,0,0,0.6)] group-hover:scale-105 transition-transform duration-500"
            />
          </div>

          <!-- CONTENIDO Y TEXTOS -->
          <div class="md:w-[58%] flex flex-col justify-center text-left md:pl-10 lg:pl-16">
            <h3
              class="text-4xl md:text-5xl font-serif font-bold text-white mb-2 drop-shadow-sm"
            >
              Bs. 29.5
            </h3>

            <h4
              class="text-xl md:text-2xl font-bold text-[#dcb041] mb-1 font-serif tracking-wide"
            >
              TABLETA DE CHOCOLATE CON QUINUA

              <span class="font-normal text-white/70 text-sm font-sans ml-1">
                100g
              </span>
            </h4>

            <p class="text-[#f6d98d]/90 italic text-sm mb-2 font-serif">
              "Para la reina de la casa"
            </p>

            <p class="text-white/80 text-sm leading-relaxed mb-6 font-sans">
              Un detalle delicado, elegante y lleno de cariño.
            </p>

            <div class="flex flex-wrap items-center gap-3 mt-auto">
              <span
                class="bg-black/40 border border-white/15 text-white/80 text-xs font-semibold px-4 py-2 rounded-full font-sans tracking-wide"
              >
                Temporada
              </span>

              <!-- Botón Añadir al Carrito con Pop-up de Cantidad -->
              <button
                type="button"
                class="add-to-cart-btn bg-[#dcb041] hover:bg-white text-[#1C0C12] text-xs font-extrabold px-5 py-2.5 rounded-full transition-all duration-300 shadow-lg hover:scale-105 cursor-pointer flex items-center gap-2"
                data-id="tableta-quinua"
                data-name="TABLETA DE CHOCOLATE CON QUINUA"
                data-size="100g"
                data-img="/img/Especial 4.png"
                data-desc="Un detalle delicado, elegante y lleno de cariño."
                data-price-cochabamba="BS. 29.5"
                data-price-lapaz="BS. 29.5"
              >
                <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" /></svg>
                <span>Añadir al Carrito</span>
              </button>

              <!-- Comprar directamente por WhatsApp -->
              <button
                type="button"
                class="direct-buy-btn bg-black/50 hover:bg-black/70 border border-white/20 text-white text-xs font-bold px-5 py-2.5 rounded-full transition-all duration-300 shadow-md hover:scale-105 text-center cursor-pointer flex items-center gap-2"
                data-name="TABLETA DE CHOC. C/ QUINUA"
                data-size="100g"
                data-price-cochabamba="BS. 29.5"
                data-price-lapaz="BS. 29.5"
              >
                Comprar <span class="text-sm">→</span>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <Products />
  <!-- SECCIÓN COMUNIDAD TABOADA (MODO OSCURO DE LUJO CON FONDO3.PNG Y TEXTOS DORADOS/CREMA) -->
  <section
    class="relative py-20 md:py-28 overflow-hidden bg-[#180a0e] border-b border-white/10 text-[#FAF5F0]"
  >
    <!-- Fondo de filigrana dorada de súper lujo proporcionado por el cliente -->
    <div
      class="absolute inset-0 bg-cover bg-center pointer-events-none opacity-90"
      style="background-image: url('/img/fondo_comunidad_dark.jpg');"
    >
    </div>

    <!-- Capa sutil de oscurecimiento y destellos -->
    <div
      class="absolute inset-0 bg-gradient-to-b from-[#180a0e]/60 via-[#120609]/40 to-[#180a0e]/70 pointer-events-none"
    >
    </div>

    <!-- Brillo dorado sutil de fondo -->
    <div
      class="absolute -left-1/4 top-1/4 w-[600px] h-[600px] rounded-full bg-[#dcb041]/10 blur-[140px] pointer-events-none"
    >
    </div>
    <div
      class="absolute -right-1/4 bottom-1/4 w-[600px] h-[600px] rounded-full bg-[#dcb041]/10 blur-[140px] pointer-events-none"
    >
    </div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      <!-- GRID SUPERIOR (2 COLUMNAS): FOTOS SUPERPUESTAS Y LISTA VERTICAL DE BENEFICIOS -->
      <div
        class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-16 items-center"
      >
        <!-- LADO IZQUIERDO (cols 1-6): Composición de 2 Fotos Superpuestas + Insignia Circular Flotante -->
        <div class="lg:col-span-6 flex justify-center relative">
          <div class="relative w-full max-w-[460px] h-[400px] sm:h-[460px]">
            <!-- 1. Foto Principal Grande (Atras / Derecha) -->
            <div
              class="absolute top-0 right-0 w-[72%] h-[85%] rounded-[2.5rem] overflow-hidden shadow-2xl border-4 border-[#dcb041]/30 z-10 bg-[#2A1612]"
            >
              <img
                src="/img/comunidad_degustacion.png"
                alt="Comunidad Taboada Degustación"
                class="w-full h-full object-cover object-center transform hover:scale-105 transition-transform duration-700"
              />
            </div>

            <!-- 2. Foto Secundaria Superpuesta (Adelante / Izquierda Abajo) -->
            <div
              class="absolute bottom-0 left-0 w-[62%] h-[68%] rounded-[2.2rem] overflow-hidden shadow-2xl border-4 border-[#dcb041]/40 z-20 bg-[#2A1612]"
            >
              <img
                src="/img/sucursal_interior.png"
                alt="Interior Chocolatería Taboada"
                class="w-full h-full object-cover object-center transform hover:scale-105 transition-transform duration-700"
              />
            </div>

            <!-- 3. Insignia Circular Flotante en la Intersección -->
            <div
              class="absolute bottom-[22%] left-[45%] -translate-x-1/2 w-24 h-24 sm:w-28 sm:h-28 rounded-full bg-gradient-to-tr from-[#2A1612] to-[#42221b] text-[#dcb041] flex flex-col items-center justify-center border-4 border-[#dcb041] shadow-2xl z-30 transform hover:scale-110 transition-transform duration-300"
            >
              <span
                class="text-lg sm:text-xl font-extrabold font-serif text-[#dcb041] leading-none"
                >1,500+</span
              >
              <span
                class="text-[9px] font-bold text-[#FAF5F0] uppercase tracking-wider mt-1 text-center px-1"
                >Miembros VIP</span
              >
            </div>
          </div>
        </div>

        <!-- LADO DERECHO (cols 7-12): Encabezado y Beneficios Verticales con Iconos Circulares -->
        <div class="lg:col-span-6 flex flex-col items-start text-left lg:pl-4">
          <!-- Eyebrow -->
          <div class="flex items-center gap-2 mb-3">
            <span class="w-8 h-[2px] bg-[#dcb041]"></span>
            <span
              class="text-xs md:text-sm uppercase tracking-widest text-[#dcb041] font-extrabold font-sans"
            >
              Programa de Lealtad
            </span>
          </div>

          <!-- Título Principal -->
          <h2
            class="font-serif text-4xl sm:text-5xl lg:text-6xl text-white leading-[1.15] mb-5 font-bold tracking-tight"
          >
            ¿Por qué unirte a <span class="text-[#dcb041] italic font-normal"
              >Comunidad Taboada?</span
            >
          </h2>

          <!-- Descripción -->
          <p
            class="text-[#FAF5F0]/85 text-sm md:text-base leading-relaxed mb-6 max-w-xl font-sans"
          >
            Sé parte de una dinastía de sabor que trasciende generaciones.
            Registrándote hoy en nuestra <strong class="text-white"
              >Comunidad Oficial</strong
            >, desbloqueas privilegios únicos y experiencias pensadas para
            amantes del auténtico chocolate.
          </p>
          <div class="space-y-5 w-full max-w-xl mb-8">
            <!-- Beneficio 1: Catas de Chocolate -->
            <div class="flex items-start gap-4">
              <div
                class="text-[#dcb041] flex items-center justify-center flex-shrink-0 pt-0.5"
              >
                <svg
                  class="w-8 h-8"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  viewBox="0 0 24 24"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M18 8h1a4 4 0 010 8h-1M2 8h16v9a4 4 0 01-4 4H6a4 4 0 01-4-4V8zM6 1v3M10 1v3M14 1v3"
                  ></path>
                </svg>
              </div>
              <div class="text-left">
                <h4 class="font-sans text-base font-bold text-white">
                  Catas de Chocolate
                </h4>
                <p
                  class="text-xs text-[#FAF5F0]/75 leading-relaxed font-sans mt-0.5"
                >
                  Invitaciones exclusivas a experiencias de maridaje y
                  degustaciones guiadas de cacao fino.
                </p>
              </div>
            </div>

            <!-- Beneficio 2: Descuentos Exclusivos -->
            <div class="flex items-start gap-4">
              <div
                class="text-[#dcb041] flex items-center justify-center flex-shrink-0 pt-0.5"
              >
                <svg
                  class="w-8 h-8"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  viewBox="0 0 24 24"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"
                  ></path>
                </svg>
              </div>
              <div class="text-left">
                <h4 class="font-sans text-base font-bold text-white">
                  Descuentos Exclusivos
                </h4>
                <p
                  class="text-xs text-[#FAF5F0]/75 leading-relaxed font-sans mt-0.5"
                >
                  Descuentos automáticos en tus compras oficiales en todas
                  nuestras sucursales.
                </p>
              </div>
            </div>

            <!-- Beneficio 3: Promociones Especiales -->
            <div class="flex items-start gap-4">
              <div
                class="text-[#dcb041] flex items-center justify-center flex-shrink-0 pt-0.5"
              >
                <svg
                  class="w-8 h-8"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  viewBox="0 0 24 24"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M12 8v13m0-13V3.75A2.25 2.25 0 009.75 1.5H7.5A2.25 2.25 0 005.25 3.75v.75m6.75 3.5V3.75A2.25 2.25 0 0114.25 1.5h2.25A2.25 2.25 0 0118.75 3.75v.75M3 8.25h18v3H3v-3zM4.5 11.25v8.25a1.5 1.5 0 001.5 1.5h12a1.5 1.5 0 001.5-1.5v-8.25"
                  ></path>
                </svg>
              </div>
              <div class="text-left">
                <h4 class="font-sans text-base font-bold text-white">
                  Promociones Especiales
                </h4>
                <p
                  class="text-xs text-[#FAF5F0]/75 leading-relaxed font-sans mt-0.5"
                >
                  Acceso a ofertas del mes, regalos de cumpleaños y colecciones
                  privadas.
                </p>
              </div>
            </div>
          </div>

          <!-- Botón de Acción Píldora Dorado de Lujo + Firma Cursiva -->
          <div class="flex flex-wrap items-center gap-6 w-full max-w-lg">
            <a
              href="/beneficios"
              class="group inline-flex items-center gap-3 p-1.5 pr-6 rounded-full bg-gradient-to-r from-[#dcb041] via-[#e5bd52] to-[#dcb041] hover:from-[#FAF5F0] hover:to-[#dcb041] text-[#2A1612] transition-all duration-500 shadow-xl hover:shadow-[0_0_30px_rgba(220,176,65,0.5)]"
            >
              <div
                class="w-10 h-10 rounded-full bg-[#2A1612] text-[#dcb041] group-hover:bg-[#2A1612] group-hover:text-[#f4c856] flex items-center justify-center font-bold transition-all duration-300 group-hover:scale-110"
              >
                <svg
                  class="w-5 h-5 group-hover:translate-x-0.5 transition-transform"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2.5"
                  viewBox="0 0 24 24"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path>
                </svg>
              </div>
              <span
                class="font-extrabold text-xs md:text-sm tracking-wider uppercase pl-1"
              >
                Unirse a la Comunidad
              </span>
            </a>

            <!-- Firma / Sello en Fuente Cursiva -->
            <div
              class="font-cursiva text-2xl md:text-3xl text-[#dcb041] font-normal tracking-wide drop-shadow-sm"
            >
              Familia Taboada
            </div>
          </div>
        </div>
      </div>

      <!-- BARRA INFERIOR DE 4 MÉTRICAS -->
      <div
        class="grid grid-cols-2 sm:grid-cols-4 gap-6 pt-12 mt-14 border-t border-white/15 w-full text-center"
      >
        <div>
          <span
            class="block text-3xl sm:text-4xl font-extrabold text-[#dcb041] font-serif tracking-tight drop-shadow-sm"
            >120+</span
          >
          <span
            class="text-[10px] sm:text-xs font-bold text-[#FAF5F0]/80 uppercase tracking-wider block mt-1"
            >Años de Historia</span
          >
        </div>
        <div>
          <span
            class="block text-3xl sm:text-4xl font-extrabold text-[#dcb041] font-serif tracking-tight drop-shadow-sm"
            >1,500+</span
          >
          <span
            class="text-[10px] sm:text-xs font-bold text-[#FAF5F0]/80 uppercase tracking-wider block mt-1"
            >Miembros Activos</span
          >
        </div>
        <div>
          <span
            class="block text-3xl sm:text-4xl font-extrabold text-[#dcb041] font-serif tracking-tight drop-shadow-sm"
            >3</span
          >
          <span
            class="text-[10px] sm:text-xs font-bold text-[#FAF5F0]/80 uppercase tracking-wider block mt-1"
            >Sucursales Oficiales</span
          >
        </div>
        <div>
          <span
            class="block text-3xl sm:text-4xl font-extrabold text-[#dcb041] font-serif tracking-tight drop-shadow-sm"
            >100%</span
          >
          <span
            class="text-[10px] sm:text-xs font-bold text-[#FAF5F0]/80 uppercase tracking-wider block mt-1"
            >Cacao Sucrense</span
          >
        </div>
      </div>
    </div>
  </section>

  <script>
    // Selectores de Categorías
    // El manejo de productos y el catálogo se encarga el componente Products.astro

    // Selectores de Ubicación
    const bgOverlay = document.getElementById("cajas-especiales-bg");
    const heroBgCover = document.getElementById("hero-bg-cover");

    // Selectores de Ver Más
    const seeMoreContainer = null;
    const seeMoreBtn = null;

    // Estados actuales
    let currentCategory = "todo";
    let currentCity = "cochabamba"; // Valor inicial / fallback
    let isExpanded = false; // Estado del botón Ver Más

    // ── MENÚ MÓVIL (DRAWER) ──
    const mobileMenuBtn = document.getElementById("mobile-menu-btn");
    const mobileCloseBtn = document.getElementById("mobile-close-btn");
    const mobileDrawer = document.getElementById("mobile-drawer");
    const drawerOverlay = document.getElementById("drawer-overlay");
    const mobileLinks = document.querySelectorAll(".mobile-link");

    function openDrawer() {
      mobileDrawer?.classList.remove("translate-x-full");
      drawerOverlay?.classList.remove("opacity-0", "pointer-events-none");
    }

    function closeDrawer() {
      mobileDrawer?.classList.add("translate-x-full");
      drawerOverlay?.classList.add("opacity-0", "pointer-events-none");
    }

    mobileMenuBtn?.addEventListener("click", (e) => {
      e.stopPropagation();
      openDrawer();
    });

    mobileCloseBtn?.addEventListener("click", closeDrawer);
    drawerOverlay?.addEventListener("click", closeDrawer);

    mobileLinks.forEach((link) => {
      link.addEventListener("click", closeDrawer);
    });

    // ── ACTUALIZAR CIUDAD SELECCIONADA ──
    function updateCity(city: string) {
      currentCity = city;
      localStorage.setItem("taboada_selected_city", city); // Changed to match Navbar

      // Inyectar clase al document.body (Para manejo CSS global si es necesario)
      document.body.classList.remove(
        "city-cochabamba",
        "city-lapaz",
        "city-la-paz",
      );
      document.body.classList.add(`city-${city}`);

      // Actualizar la portada del Hero con una transición (Fade Out / Fade In)
      if (heroBgCover) {
        const bgCbba =
          heroBgCover.getAttribute("data-bg-cochabamba") ||
          "/img/portadataboada.png";
        const bgLpz =
          heroBgCover.getAttribute("data-bg-lapaz") || "/img/portadalapaz.png";

        // Transición ultra suave de opacidad (fade-out / fade-in) y transform con curva cubic-bezier premium
        heroBgCover.style.transition =
          "opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1), transform 1.2s cubic-bezier(0.4, 0, 0.2, 1)";
        heroBgCover.style.opacity = "0"; // Disparar fade-out

        setTimeout(() => {
          if (city === "cochabamba") {
            heroBgCover.style.backgroundImage = `url('${bgCbba}')`;
          } else if (city === "la-paz") {
            heroBgCover.style.backgroundImage = `url('${bgLpz}')`;
          }
          heroBgCover.style.opacity = "1"; // Disparar fade-in tras aplicar nueva imagen
        }, 500); // Sincronizado con los 500ms del desvanecimiento cubic-bezier
      }

      // Actualizar el degradado de fondo de Colecciones
      if (bgOverlay) {
        // Remover clases viejas de gradientes
        bgOverlay.classList.remove(
          "from-[#FAF5F0]",
          "via-[#F3ECE3]",
          "to-[#FAF5F0]",
          "from-[#FAF6F2]",
          "via-[#EADCD0]",
          "to-[#FAF6F2]",
          "from-[#FAF7F5]",
          "via-[#E2DFD8]",
          "to-[#FAF7F5]",
        );

        // Añadir clases nuevas basadas en la ciudad y aplicar transición
        bgOverlay.style.transition = "all 0.6s ease-in-out";
        if (city === "cochabamba") {
          bgOverlay.classList.add(
            "from-[#FAF6F2]",
            "via-[#EADCD0]",
            "to-[#FAF6F2]",
          );
        } else if (city === "la-paz") {
          bgOverlay.classList.add(
            "from-[#FAF7F5]",
            "via-[#E2DFD8]",
            "to-[#FAF7F5]",
          );
        }
      }

      // Refrescar el filtrado de productos y los precios
      // filterProducts();
    }

    // ── MANTENER SINCRONIZADA LA IMAGEN HERO SEGÚN LA CIUDAD ──

    // Escuchar cambios de ciudad desde Navbar
    window.addEventListener("cityChanged", (e) => {
      const detail = (e as CustomEvent).detail;
      if (detail && detail.city) {
        updateCity(detail.city);
      }
    });

    // ── LÓGICA DE COLAPSAR/GUARDAR BARRA LATERAL DE CATEGORÍAS ──
    const catSidebar = document.getElementById("cat-sidebar");
    const toggleSidebarBtn = document.getElementById("toggle-cat-sidebar-btn");
    const arrowIcon = document.getElementById("cat-sidebar-arrow-icon");
    const headerTitle = document.getElementById("cat-sidebar-header-title");
    const productGridColumn = document.getElementById("product-grid-column");

    toggleSidebarBtn?.addEventListener("click", () => {
      const isCollapsed = catSidebar?.classList.contains(
        "is-collapsed-sidebar",
      );

      if (isCollapsed) {
        // EXPANDIR
        catSidebar?.classList.remove("is-collapsed-sidebar");
        catSidebar?.classList.remove("lg:col-span-1", "lg:w-[70px]");
        catSidebar?.classList.add("lg:col-span-2", "w-full");

        productGridColumn?.classList.remove("lg:col-span-11");
        productGridColumn?.classList.add("lg:col-span-10");

        if (arrowIcon) arrowIcon.style.transform = "rotate(0deg)";
        if (headerTitle) headerTitle.style.display = "";

        document
          .querySelectorAll(".cat-btn-label")
          .forEach((el) => ((el as HTMLElement).style.display = ""));
        document.querySelectorAll(".cat-btn").forEach((btn) => {
          btn.classList.remove("justify-center");
          btn.classList.add("gap-2.5");
        });
      } else {
        // COLAPSAR / GUARDAR (MODO SOLO ÍCONOS)
        catSidebar?.classList.add("is-collapsed-sidebar");
        catSidebar?.classList.remove("lg:col-span-2", "w-full");
        catSidebar?.classList.add("lg:col-span-1", "lg:w-[70px]");

        productGridColumn?.classList.remove("lg:col-span-10");
        productGridColumn?.classList.add("lg:col-span-11");

        if (arrowIcon) arrowIcon.style.transform = "rotate(180deg)";
        if (headerTitle) headerTitle.style.display = "none";

        document
          .querySelectorAll(".cat-btn-label")
          .forEach((el) => ((el as HTMLElement).style.display = "none"));
        document.querySelectorAll(".cat-btn").forEach((btn) => {
          btn.classList.add("justify-center");
          btn.classList.remove("gap-2.5");
        });
      }
    });

    // ── INICIALIZACIÓN Y DETECCIÓN POR IP SILENCIOSA ──
    const savedCity = localStorage.getItem("taboada_selected_city");
    if (savedCity && (savedCity === "cochabamba" || savedCity === "la-paz")) {
      updateCity(savedCity);
    } else {
      // Petición silenciosa asíncrona a ipapi.co/json/ (HTTPS)
      fetch("https://ipapi.co/json/")
        .then((res) => {
          if (res.ok) return res.json();
          throw new Error("API Response Error");
        })
        .then((data) => {
          console.log("Ciudad detectada por API:", data.city);

          const cityValue = (data.city || "").toLowerCase();
          const regionValue = (data.region || "").toLowerCase();

          // Evaluación estricta única y exclusivamente de los campos city y region (ignora timezone)
          const detected =
            cityValue.includes("paz") || regionValue.includes("paz")
              ? "la-paz"
              : "cochabamba";
          updateCity(detected);
        })
        .catch((err) => {
          // Manejo silencioso de errores para evitar romper la carga (AdBlockers o fallos de red)
          updateCity("cochabamba");
        });
    }

    // ── DETECTAR Y FILTRAR POR PARÁMETROS URL (?cat=...) ──
    function checkUrlParams() {
      const urlParams = new URLSearchParams(window.location.search);
      const catParam = urlParams.get("cat");
      if (catParam) {
        const targetBtn = document.querySelector(
          `.cat-btn[data-target="${catParam}"]`,
        ) as HTMLButtonElement;
        if (targetBtn) {
          setTimeout(() => {
            targetBtn.click();
            const targetSection = document.getElementById("cajas-especiales");
            if (targetSection) {
              targetSection.scrollIntoView({
                behavior: "smooth",
                block: "start",
              });
            }
          }, 300);
        }
      }
    }

    checkUrlParams();

    // Interceptar clicks en enlaces del menu dropdown cuando estamos en el home
    const dropdownLinks = document.querySelectorAll('a[href*="?cat="]');
    dropdownLinks.forEach((link) => {
      link.addEventListener("click", (e) => {
        const hrefAttr = link.getAttribute("href") || "";
        const isHomePage =
          window.location.pathname === "/" ||
          window.location.pathname === "/index.html" ||
          window.location.pathname === "";
        if (isHomePage) {
          const url = new URL(hrefAttr, window.location.href);
          const cat = url.searchParams.get("cat");
          if (cat) {
            const targetBtn = document.querySelector(
              `.cat-btn[data-target="${cat}"]`,
            ) as HTMLButtonElement;
            if (targetBtn) {
              e.preventDefault();
              targetBtn.click();
              const targetSection = document.getElementById("cajas-especiales");
              if (targetSection) {
                targetSection.scrollIntoView({
                  behavior: "smooth",
                  block: "start",
                });
              }
              // Limpiar la query de la URL sin recargar
              window.history.pushState({}, "", window.location.pathname);
            }
          }
        }
      });
    });

    // ── GESTIÓN DEL MODAL DE CANTIDAD Y BOTONES DE COMPRA ──
    const qtyModal = document.getElementById("quantity-modal");
    const qtyModalOverlay = document.getElementById("quantity-modal-overlay");
    const qtyModalClose = document.getElementById("quantity-modal-close");
    const modalProdName = document.getElementById("modal-product-name");
    const modalProdSize = document.getElementById("modal-product-size");
    const modalProdImg = document.getElementById("modal-product-img") as HTMLImageElement;
    const modalQtyVal = document.getElementById("modal-qty-val");
    const modalQtyMinus = document.getElementById("modal-qty-minus");
    const modalQtyPlus = document.getElementById("modal-qty-plus");
    const modalAddBtn = document.getElementById("modal-add-btn");
    const floatingCartBtn = document.getElementById("floating-cart-btn");

    let modalActiveProduct = {
      id: "",
      name: "",
      size: "",
      img: "",
      price: 0,
      quantity: 1,
    };

    function parsePrice(priceStr: string): number {
      const cleanStr = priceStr
        .replace(/BS\./i, "")
        .replace(/Bs\./i, "")
        .replace(/BS/i, "")
        .replace(/Bs/i, "");
      return parseFloat(cleanStr.replace(/[^\d.]/g, "")) || 0;
    }

    const modalProdDesc = document.getElementById("modal-product-desc");

    function updateQtyModalUI() {
      if (modalQtyVal) modalQtyVal.textContent = modalActiveProduct.quantity.toString();
      const subtotal = modalActiveProduct.price * modalActiveProduct.quantity;
      const modalTotalPrice = document.getElementById("modal-total-price");
      if (modalTotalPrice) modalTotalPrice.textContent = `Bs. ${subtotal.toFixed(2)}`;
      const modalAddBtnText = document.getElementById("modal-add-btn-text");
      if (modalAddBtnText) modalAddBtnText.textContent = `Agregar al Carrito • Bs. ${subtotal.toFixed(2)}`;
    }

    function openQtyModal(
      id: string,
      name: string,
      size: string,
      img: string,
      price: number,
      desc: string = "",
    ) {
      modalActiveProduct = { id, name, size, img, price, quantity: 1 };

      if (modalProdName) modalProdName.textContent = name;
      if (modalProdSize) modalProdSize.textContent = size;
      if (modalProdImg) modalProdImg.src = img;
      if (modalProdDesc) {
        if (desc && desc.trim() !== "") {
          modalProdDesc.textContent = desc;
          modalProdDesc.classList.remove("hidden");
        } else {
          modalProdDesc.textContent = "";
          modalProdDesc.classList.add("hidden");
        }
      }
      
      updateQtyModalUI();

      qtyModalOverlay?.classList.remove("hidden");
      qtyModal?.classList.remove("hidden");

      setTimeout(() => {
        qtyModal?.classList.remove("scale-95", "opacity-0");
        qtyModal?.classList.add("scale-100", "opacity-100");
      }, 10);
    }

    function closeQtyModal() {
      qtyModal?.classList.remove("scale-100", "opacity-100");
      qtyModal?.classList.add("scale-95", "opacity-0");

      setTimeout(() => {
        qtyModal?.classList.add("hidden");
        qtyModalOverlay?.classList.add("hidden");
      }, 300);
    }

    qtyModalClose?.addEventListener("click", closeQtyModal);
    qtyModalOverlay?.addEventListener("click", closeQtyModal);

    modalQtyMinus?.addEventListener("click", () => {
      if (modalActiveProduct.quantity > 1) {
        modalActiveProduct.quantity -= 1;
        updateQtyModalUI();
      }
    });

    modalQtyPlus?.addEventListener("click", () => {
      modalActiveProduct.quantity += 1;
      updateQtyModalUI();
    });

    modalAddBtn?.addEventListener("click", () => {
      if (typeof (window as any).addToCart === "function") {
        (window as any).addToCart({
          id: modalActiveProduct.id || modalActiveProduct.name.toLowerCase().replace(/\s+/g, "-"),
          name: modalActiveProduct.name,
          size: modalActiveProduct.size,
          img: modalActiveProduct.img,
          price: modalActiveProduct.price,
          quantity: modalActiveProduct.quantity,
        });
      }
      closeQtyModal();
    });

    // Enlaces de botones y tarjetas para agregar al carrito (Delegado dinámicamente al hacer clic en la tarjeta o botón)
    document.addEventListener("click", (e) => {
      const clickedEl = e.target as HTMLElement;

      // Si hace clic en el botón direct-buy-btn ("Comprar" vía WhatsApp directo), ignorar apertura de modal
      if (clickedEl.closest(".direct-buy-btn")) return;

      const target = clickedEl.closest(".product-card, .add-to-cart-btn") as HTMLElement | null;
      if (!target) return;

      e.preventDefault();
      const name = target.getAttribute("data-name") || "Chocolate Taboada";
      const size = target.getAttribute("data-size") || "Edición Especial";
      const img = target.getAttribute("data-img") || "/img/produc1.png";
      const desc = target.getAttribute("data-desc") || "Elaborado artesanalmente con 100% cacao fino de aroma sucrense y la más alta tradición chocolatera de Bolivia.";

      const selectedCity = (localStorage.getItem("taboada_selected_city") || "cochabamba").toLowerCase();
      const priceKey = (selectedCity === "la-paz" || selectedCity === "lapaz") ? "data-price-lapaz" : "data-price-cochabamba";
      const priceStr =
        target.getAttribute(priceKey) ||
        target.getAttribute("data-price") ||
        target.getAttribute("data-price-cochabamba") ||
        "75";
      const price = parsePrice(priceStr);

      const id = target.getAttribute("data-id") || name.toLowerCase().replace(/\s+/g, "-");

      openQtyModal(id, name, size, img, price, desc);
    });

    // Compra directa WhatsApp de un solo producto
    const directBuyButtons = document.querySelectorAll(".direct-buy-btn");
    directBuyButtons.forEach((btn) => {
      btn.addEventListener("click", (e) => {
        const target = e.currentTarget as HTMLButtonElement;
        const name = target.getAttribute("data-name") || "";
        const size = target.getAttribute("data-size") || "";

        const priceKey =
          currentCity === "cochabamba"
            ? "data-price-cochabamba"
            : "data-price-lapaz";
        const priceStr =
          target.getAttribute(priceKey) ||
          target.getAttribute("data-price-cochabamba") ||
          "";
        const priceVal = parsePrice(priceStr);

        const cityLabel =
          currentCity === "cochabamba" ? "Cochabamba" : "La Paz";

        let message = `¡Hola! Quisiera comprar el siguiente producto de Chocolates Taboada:\n\n`;
        message += `Ciudad de entrega: ${cityLabel}\n`;
        message += `------------------------------------------\n`;
        message += `• 1 x ${name} (${size})\n`;
        message += `  Precio unitario: BS. ${priceVal.toFixed(1)}\n`;
        message += `  Subtotal: BS. ${priceVal.toFixed(1)}\n`;
        message += `------------------------------------------\n`;
        message += `Total a pagar: BS. ${priceVal.toFixed(1)}\n\n`;
        message += `¡Muchas gracias!`;

        const waNumber = "59179786916";
        const encodedMsg = encodeURIComponent(message);
        const waLink = `https://wa.me/${waNumber}?text=${encodedMsg}`;

        window.open(waLink, "_blank");
      });
    });

    floatingCartBtn?.addEventListener("click", () => {
      if (typeof (window as any).openCartDrawer === "function") {
        (window as any).openCartDrawer();
      }
    });

    // ── CAMBIO DE COLOR DEL NAVBAR AL HACER SCROLL ──
    const navbar = document.getElementById("main-navbar");
    window.addEventListener("scroll", () => {
      if (window.scrollY > 50) {
        navbar?.classList.add(
          "bg-[#2A1612]/95",
          "backdrop-blur-[14px]",
          "border-b",
          "border-white/10",
          "shadow-lg",
        );
        navbar?.classList.remove(
          "bg-transparent",
          "border-transparent",
          "hover:bg-white/10",
        );
      } else {
        navbar?.classList.remove(
          "bg-[#2A1612]/95",
          "backdrop-blur-[14px]",
          "border-b",
          "border-white/10",
          "shadow-lg",
        );
        navbar?.classList.add(
          "bg-transparent",
          "border-transparent",
          "hover:bg-white/10",
        );
      }
    });
  </script>

  <style>
    @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;1,400&family=Pinyon+Script&display=swap");

    .font-elegante {
      font-family: "Playfair Display", serif;
    }

    .font-cursiva {
      font-family: "Pinyon Script", cursive;
    }
  </style>

  <!-- ======================================================== -->
  <!-- COMPONENTES DEL CARRITO DE COMPRA E INTERACTIVOS -->
  <!-- ======================================================== -->

  <!-- BOTÓN FLOTANTE DEL CARRITO -->
  <button
    id="floating-cart-btn"
    class="fixed bottom-6 right-6 z-[990] w-14 h-14 rounded-full bg-[#dcb041] hover:bg-[#FAF5F0] border border-[#dcb041] text-[#2A1612] flex items-center justify-center shadow-[0_4px_25px_rgba(220,176,65,0.45)] hover:scale-105 active:scale-95 transition-all duration-300 cursor-pointer"
  >
    <svg
      class="w-6 h-6"
      fill="none"
      stroke="currentColor"
      stroke-width="2.5"
      viewBox="0 0 24 24"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z"
      ></path>
    </svg>
    <span
      id="cart-count-badge"
      class="absolute -top-1 -right-1 bg-[#e53e3e] text-white text-[10px] font-extrabold w-5 h-5 rounded-full flex items-center justify-center border border-white"
    >
      0
    </span>
  </button>

  <!-- OVERLAY Y MODAL DE SELECCIÓN DE CANTIDAD (POP-UP AMPLIADO CON INFORMACIÓN DE PRODUCTO) -->
  <div
    id="quantity-modal-overlay"
    class="fixed inset-0 bg-black/75 z-[10000] hidden backdrop-blur-md transition-all duration-300"
  >
  </div>
  <div
    id="quantity-modal"
    class="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[92%] max-w-lg sm:max-w-xl bg-[#FAF5F0] border border-[#dcb041]/40 rounded-[2.5rem] shadow-[0_25px_60px_rgba(0,0,0,0.7)] p-6 sm:p-8 z-[10001] hidden flex flex-col items-center justify-between transition-all duration-300 scale-95 opacity-0 text-center max-h-[90vh] overflow-y-auto"
  >
    <!-- Botón de Cerrar Modal -->
    <button
      id="quantity-modal-close"
      type="button"
      class="absolute top-4 right-4 w-9 h-9 rounded-full flex items-center justify-center bg-[#2A1612]/10 hover:bg-[#2A1612] text-[#2A1612] hover:text-[#dcb041] transition-all duration-200 cursor-pointer font-bold"
    >
      ✕
    </button>

    <div class="w-full text-center">
      <span class="inline-block px-3 py-1 rounded-full bg-[#dcb041]/20 text-[#7a4e2b] text-[10px] font-black uppercase tracking-wider mb-2">Detalles del Producto</span>
      <h3
        id="modal-product-name"
        class="text-xl sm:text-2xl font-bold font-serif text-[#2A1612] leading-tight"
      >
      </h3>
      <span
        id="modal-product-size"
        class="block text-xs text-[#8f6c49] font-sans mt-1 font-bold"></span>

      <!-- Imagen del Producto -->
      <div class="w-full flex justify-center my-4">
        <div class="w-40 h-40 sm:w-48 sm:h-48 rounded-2xl bg-white p-3 border border-[#ecddd0] shadow-inner flex items-center justify-center">
          <img
            id="modal-product-img"
            src=""
            alt="Producto"
            class="max-w-full max-h-full object-contain filter drop-shadow-md"
          />
        </div>
      </div>

      <!-- Breve Información / Descripción del Producto -->
      <p
        id="modal-product-desc"
        class="text-xs sm:text-sm text-[#5D4037]/90 font-sans leading-relaxed my-3 bg-white/70 p-3 sm:p-4 rounded-2xl border border-[#ecddd0] text-center"
      >
      </p>

      <!-- Selector de cantidad pill -->
      <div class="flex items-center justify-center gap-5 my-3">
        <button
          id="modal-qty-minus"
          type="button"
          class="w-11 h-11 rounded-full border border-[#2A1612]/20 flex items-center justify-center font-black text-xl text-[#2A1612] bg-white hover:bg-[#2A1612] hover:text-[#dcb041] active:scale-95 transition-all shadow-sm cursor-pointer"
        >
          -
        </button>
        <span
          id="modal-qty-val"
          class="w-12 text-center text-2xl font-black text-[#2A1612] font-serif">1</span
        >
        <button
          id="modal-qty-plus"
          type="button"
          class="w-11 h-11 rounded-full border border-[#2A1612]/20 flex items-center justify-center font-black text-xl text-[#2A1612] bg-white hover:bg-[#2A1612] hover:text-[#dcb041] active:scale-95 transition-all shadow-sm cursor-pointer"
        >
          +
        </button>
      </div>

      <!-- Subtotal Precio -->
      <div class="text-center mt-2 mb-4">
        <span class="text-xs text-[#8f6c49] font-medium uppercase tracking-wider block">Total Estimado</span>
        <span id="modal-total-price" class="text-2xl font-serif font-black text-[#2A1612]">Bs. 0.00</span>
      </div>

      <!-- Botón de agregar -->
      <button
        id="modal-add-btn"
        type="button"
        class="w-full py-4 rounded-full bg-gradient-to-r from-[#dcb041] via-[#f4c856] to-[#dcb041] text-[#2A1612] font-black text-sm uppercase tracking-wide shadow-lg hover:shadow-[0_6px_25px_rgba(220,176,65,0.5)] hover:scale-[1.02] active:scale-95 transition-all duration-300 cursor-pointer flex items-center justify-center gap-2"
      >
        <span id="modal-add-btn-text">Agregar al Carrito</span>
      </button>
    </div>
  </div>
  <CallToAction />
  <Testimonials />
  <Footer />
  <MobileCTA />
</Layout>
