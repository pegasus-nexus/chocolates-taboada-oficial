---
import Layout from "../layouts/Layout.astro";
import CallToAction from "../components/CallToAction.astro";
import Testimonials from "../components/Testimonials.astro";
import Footer from "../components/Footer.astro";
import MobileCTA from "../components/MobileCTA.astro";

const categories = [
  { id: 'todo', label: 'Todo' },
  { id: 'blister', label: 'Blister' },
  { id: 'bombones', label: 'Bombones' },
  { id: 'cajas', label: 'Cajas' },
  { id: 'grageas', label: 'Grageas' },
  { id: 'paletas', label: 'Paletas' },
  { id: 'postal', label: 'Postal' },
  { id: 'reposteria', label: 'Repostería' },
  { id: 'sin-azucar', label: 'Sin Azúcar' },
  { id: 'tabletas', label: 'Tabletas' },
  { id: 'turrones', label: 'Turrones' },
  { id: 'variados', label: 'Variados' }
];

const products = [
  {
    name: "Caja Roja Corazón",
    size: "100 g",
    desc: "Con corazones de chocolate de leche.",
    price: { cochabamba: "BS. 77", "la-paz": "BS. 82" },
    available: ["cochabamba", "la-paz"],
    category: "cajas",
    img: "/img/produc2.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Roja%20Corazón%20de%20100g"
  },
  {
    name: "Caja roja de Corazón",
    size: "180 g",
    desc: "Presentación rectangular premium.",
    price: { cochabamba: "BS. 81", "la-paz": "BS. 86" },
    available: ["cochabamba", "la-paz"],
    category: "cajas",
    img: "/img/produc1.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20roja%20de%20Corazón%20de%20180g"
  },
  {
    name: "Caja Lila",
    size: "200 g",
    desc: "Edición especial Felicidades.",
    price: { cochabamba: "BS. 63", "la-paz": "BS. 68" },
    available: ["cochabamba", "la-paz"],
    category: "cajas",
    img: "/img/produc3.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Lila%20de%20200g"
  },
  {
    name: "Caja Paleta",
    size: "100 g",
    desc: "Con diseño floral de rosas.",
    price: { cochabamba: "BS. 63", "la-paz": "BS. 67" },
    available: ["cochabamba"],
    category: "cajas paletas",
    img: "/img/produc4.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Paleta%20de%20100g"
  },
  {
    name: "Caja Surtida",
    size: "122 g",
    desc: "Bombones con rellenos finos.",
    price: { cochabamba: "BS. 63", "la-paz": "BS. 67" },
    available: ["cochabamba", "la-paz"],
    category: "cajas bombones",
    img: "/img/produc5.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Surtida%20de%20122g"
  },
  {
    name: "Caja Tumbo",
    size: "200 g",
    desc: "Chocolate sabor tradicional de Sucre.",
    price: { cochabamba: "BS. 63", "la-paz": "BS. 63" },
    available: ["la-paz"],
    category: "cajas",
    img: "/img/produc6.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Tumbo%20de%20200g"
  },
  {
    name: "Caja Rosada",
    size: "180 g",
    desc: "Con corazones en relieve.",
    price: { cochabamba: "BS. 74", "la-paz": "BS. 79" },
    available: ["cochabamba", "la-paz"],
    category: "cajas",
    img: "/img/produc7.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Caja%20Rosada%20de%20180g"
  },
  {
    name: "Tableta Chocolate con Leche",
    size: "100 g",
    desc: "Tres diseños tradicionales coleccionables.",
    price: { cochabamba: "BS. 29.5", "la-paz": "BS. 34" },
    available: ["cochabamba", "la-paz"],
    category: "tabletas",
    img: "/img/tableta1.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Tableta%20Chocolate%20con%20Leche%20de%20100g"
  },
  {
    name: "Tableta Bitter Sin Azúcar",
    size: "80 g",
    desc: "70% Cacao fino de aroma sin azúcar añadida.",
    price: { cochabamba: "BS. 35", "la-paz": "BS. 40" },
    available: ["cochabamba", "la-paz"],
    category: "sin-azucar tabletas",
    img: "/img/tableta2.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Tableta%20Bitter%20Sin%20Azucar"
  },
  {
    name: "Postal Chocolate Sucre",
    size: "90 g",
    desc: "Recuerdo dulce con imágenes de la ciudad blanca.",
    price: { cochabamba: "BS. 22", "la-paz": "BS. 25" },
    available: ["cochabamba", "la-paz"],
    category: "postal",
    img: "/img/tableta3.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Postal%20Chocolate%20Sucre"
  },
  {
    name: "Paleta Corazón Grande",
    size: "34 g",
    desc: "Detalle romántico y elegante.",
    price: { cochabamba: "BS. 14", "la-paz": "BS. 17" },
    available: ["cochabamba", "la-paz"],
    category: "paletas",
    img: "/img/paleta1.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Paleta%20Corazón%20de%2034g"
  },
  {
    name: "Paleta Rosa con Tarjeta",
    size: "26 g",
    desc: "Paleta con tarjeta de felicitación.",
    price: { cochabamba: "BS. 12", "la-paz": "BS. 15" },
    available: ["cochabamba", "la-paz"],
    category: "paletas",
    img: "/img/paleta2.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20la%20Paleta%20Rosa%20de%2026g"
  },
  {
    name: "Blíster Bombones Surtidos",
    size: "60 g",
    desc: "Selección rápida de nuestros mejores bombones.",
    price: { cochabamba: "BS. 18", "la-paz": "BS. 22" },
    available: ["cochabamba", "la-paz"],
    category: "blister bombones",
    img: "/img/produc5.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20el%20Blister%20Bombones%20Surtidos"
  },
  {
    name: "Pack Grageas Clásicas",
    size: "150 g",
    desc: "Uvas pasas y maní cubiertos con chocolate de leche.",
    price: { cochabamba: "BS. 54", "la-paz": "BS. 59" },
    available: ["cochabamba"],
    category: "grageas",
    img: "/img/prom2.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20el%20Pack%20Grageas%20Clasicas"
  },
  {
    name: "Chocolate Taza Tradicional",
    size: "200 g",
    desc: "Ideal para preparar chocolate caliente al estilo Sucre.",
    price: { cochabamba: "BS. 25", "la-paz": "BS. 28" },
    available: ["cochabamba", "la-paz"],
    category: "reposteria",
    img: "/img/especial1.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20el%20Chocolate%20Taza%20Tradicional"
  },
  {
    name: "Turrón Crocante de Almendras",
    size: "100 g",
    desc: "Turrón artesanal cubierto de chocolate de leche.",
    price: { cochabamba: "BS. 28", "la-paz": "BS. 32" },
    available: ["cochabamba", "la-paz"],
    category: "turrones",
    img: "/img/tableta1.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20el%20Turron%20de%20Almendras"
  },
  {
    name: "Pack Selección Especial",
    size: "450 g",
    desc: "Surtido variado de tabletas, bombones y grageas.",
    price: { cochabamba: "BS. 110", "la-paz": "BS. 120" },
    available: ["cochabamba", "la-paz"],
    category: "variados",
    img: "/img/prom2.png",
    link: "https://wa.me/59179786916?text=Hola%2C%20quiero%20comprar%20el%20Pack%20Seleccion%20Especial"
  }
];
---

<Layout
  title="El sabor de la tradición | Chocolates Taboada"
  description="Descubre Chocolates Taboada, la primera industria chocolatera de Bolivia desde 1948. Tradición, elegancia y un sabor inolvidable."
>
  <nav
    id="main-navbar"
    class="fixed top-0 left-0 w-full z-[999] transition-all duration-500
         bg-transparent border-b border-transparent hover:bg-white/10"
  >
    <div class="max-w-[1280px] mx-auto flex items-center justify-between h-[85px] md:h-[95px] px-6 lg:px-12 relative">
      
      <!-- LOGO IZQUIERDO -->
      <div class="flex items-center">
        <a href="/">
          <img
            src="/img/taboada-logo.png"
            alt="Taboada"
            class="h-[50px] md:h-[65px] object-contain transition-transform duration-300 hover:scale-105"
          />
        </a>
      </div>

      <!-- ENLACES CENTRALES (Desktop) -->
      <div class="hidden md:flex items-center gap-8 lg:gap-10">
        <!-- INICIO -->
        <a
          href="#inicio"
          class="relative group py-2 text-white/90 hover:text-[#dcb041] font-bold text-sm tracking-wide transition-colors duration-300"
        >
          Inicio
          <span class="absolute bottom-[-2px] left-1/2 -translate-x-1/2 w-1.5 h-1.5 bg-[#dcb041] rounded-full scale-0 group-hover:scale-100 transition-all duration-300 ease-out"></span>
        </a>
        <a
          href="/nosotros"
          class="relative group py-2 text-white/90 hover:text-[#dcb041] font-bold text-sm tracking-wide transition-colors duration-300"
        >
          Nosotros
          <span class="absolute bottom-[-2px] left-1/2 -translate-x-1/2 w-1.5 h-1.5 bg-[#dcb041] rounded-full scale-0 group-hover:scale-100 transition-all duration-300 ease-out"></span>
        </a>

        <!-- COLECCIONES (Dropdown) -->
        <div class="relative group py-2">
          <button class="flex items-center gap-1 text-white/90 hover:text-[#dcb041] font-bold text-sm tracking-wide transition-colors duration-300 cursor-pointer">
            Colecciones
            <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover:rotate-180" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"></path></svg>
          </button>
          <span class="absolute bottom-[-2px] left-1/2 -translate-x-1/2 w-1.5 h-1.5 bg-[#dcb041] rounded-full scale-0 group-hover:scale-100 transition-all duration-300 ease-out"></span>
          
          <!-- CONTENIDO DROPDOWN -->
          <div class="absolute left-1/2 -translate-x-1/2 mt-3 w-80 rounded-2xl bg-[#2A1612]/95 border border-white/10 shadow-2xl backdrop-blur-md overflow-hidden opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[1000] translate-y-2 group-hover:translate-y-0">
            <div class="p-3 grid grid-cols-1 gap-1">
              <a href="/?cat=bombones" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <div>
                  <div class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Bombones Finos</div>
                  <div class="text-xs text-white/60 mt-0.5">Elegancia y variedad de sabores premium</div>
                </div>
              </a>
              <a href="/?cat=grageas" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <div>
                  <div class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Cajas y Grageas</div>
                  <div class="text-xs text-white/60 mt-0.5">Detalles perfectos y crujientes de sabor</div>
                </div>
              </a>
              <a href="/?cat=tabletas" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <div>
                  <div class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Tabletas y Barras</div>
                  <div class="text-xs text-white/60 mt-0.5">Chocolate de origen seleccionado</div>
                </div>
              </a>
              <a href="/?cat=reposteria" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <div>
                  <div class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Chocolate de Taza</div>
                  <div class="text-xs text-white/60 mt-0.5">La receta familiar tradicional boliviana</div>
                </div>
              </a>
            </div>
          </div>
        </div>

        <!-- BENEFICIOS (Dropdown) -->
        <div class="relative group py-2">
          <button class="flex items-center gap-1 text-white/90 hover:text-[#dcb041] font-bold text-sm tracking-wide transition-colors duration-300 cursor-pointer">
            Beneficios
            <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover:rotate-180" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"></path></svg>
          </button>
          <span class="absolute bottom-[-2px] left-1/2 -translate-x-1/2 w-1.5 h-1.5 bg-[#dcb041] rounded-full scale-0 group-hover:scale-100 transition-all duration-300 ease-out"></span>

          <!-- CONTENIDO DROPDOWN -->
          <div class="absolute left-1/2 -translate-x-1/2 mt-3 w-80 rounded-2xl bg-[#2A1612]/95 border border-white/10 shadow-2xl backdrop-blur-md overflow-hidden opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[1000] translate-y-2 group-hover:translate-y-0">
            <div class="p-3 grid grid-cols-1 gap-1">
              <a href="/beneficios" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <span class="text-xl">✨</span>
                <div>
                  <div class="flex items-center gap-2">
                    <span class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Club Taboada</span>
                    <span class="px-1.5 py-0.5 text-[9px] bg-[#dcb041]/20 text-[#dcb041] font-extrabold rounded">Socio</span>
                  </div>
                  <div class="text-xs text-white/60 mt-0.5">Beneficios exclusivos y catas de chocolate</div>
                </div>
              </a>
              <a href="/adulto-mayor" class="flex items-start gap-3 p-3 rounded-xl hover:bg-white/5 transition-all group/item">
                <span class="text-xl">💖</span>
                <div>
                  <div class="text-sm font-bold text-white group-hover/item:text-[#dcb041] transition-colors">Membresía Legado</div>
                  <div class="text-xs text-white/60 mt-0.5">Atención preferencial y 10% de descuento</div>
                </div>
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- DERECHA (Selector Ciudad + CTA Pedir Ahora + Hamburger) -->
      <div class="flex items-center gap-3 md:gap-4 relative overflow-visible">
        
        <!-- SELECTOR DE CIUDAD -->
        <div class="relative inline-block text-left z-[1000] overflow-visible">
          <button
            id="city-selector-btn"
            type="button"
            class="flex items-center gap-1.5 px-4 py-2 text-xs md:text-sm font-bold rounded-full bg-white/10 text-white hover:bg-white/20 border border-white/20 transition-all duration-300 cursor-pointer shadow-lg hover:scale-[1.03]"
          >
            <svg class="w-4 h-4 text-[#dcb041]" fill="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"></path>
            </svg>
            <span id="active-city-name">Detectando...</span>
            <span class="text-[8px] opacity-75">▼</span>
          </button>
          
          <!-- MENU DESPLEGABLE CIUDAD -->
          <div
            id="city-dropdown"
            class="hidden absolute right-0 mt-2 w-44 rounded-2xl bg-[#2A1612]/95 border border-white/10 shadow-2xl backdrop-blur-md overflow-hidden transition-all duration-300"
          >
            <button data-city="cochabamba" class="city-opt-btn w-full px-4 py-3 text-left text-xs text-white hover:bg-[#dcb041] hover:text-[#2e1119] transition-colors duration-250 cursor-pointer block font-semibold border-b border-white/5">📍 Cochabamba</button>
            <button data-city="la-paz" class="city-opt-btn w-full px-4 py-3 text-left text-xs text-white hover:bg-[#dcb041] hover:text-[#2e1119] transition-colors duration-250 cursor-pointer block font-semibold">📍 La Paz</button>
          </div>
        </div>

        <!-- CTA PEDIR AHORA -->
        <a
          href="https://wa.me/59179786916?text=Hola%2C%20quiero%20hacer%20un%20pedido" 
          target="_blank"
          class="hidden sm:inline-flex items-center justify-center bg-gradient-to-r from-[#dcb041] to-[#f4c856] text-[#2A1612] font-extrabold text-xs md:text-sm px-6 py-2.5 rounded-full shadow-[0_4px_18px_rgba(220,176,65,0.35)] hover:shadow-[0_6px_22px_rgba(220,176,65,0.5)] transition-all duration-300 transform hover:scale-[1.03] active:scale-[0.98]"
        >
          Pedir Ahora
        </a>

        <!-- BOTÓN MENÚ MÓVIL (Hamburger) -->
        <button
          id="mobile-menu-btn"
          class="md:hidden flex items-center justify-center w-10 h-10 rounded-full bg-white/10 border border-white/20 text-white hover:bg-white/25 transition-all duration-300 cursor-pointer"
          aria-label="Abrir menú"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 8h16M4 16h16"></path>
          </svg>
        </button>

      </div>

    </div>

    <!-- MENÚ MÓVIL (Drawer deslizante) -->
    <div
      id="mobile-drawer"
      class="fixed inset-y-0 right-0 z-[1000] w-[290px] bg-[#2A1612]/98 backdrop-blur-lg border-l border-white/10 shadow-2xl p-6 transform translate-x-full transition-transform duration-300 flex flex-col justify-between"
    >
      <div>
        <div class="flex items-center justify-between pb-6 border-b border-white/10">
          <img src="/img/taboada-logo.png" alt="Taboada" class="h-10 object-contain" />
          <button
            id="mobile-close-btn"
            class="text-white/70 hover:text-white transition-colors cursor-pointer"
            aria-label="Cerrar menú"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>

        <div class="flex flex-col gap-6 mt-8">
          <a href="#inicio" class="mobile-link text-white/90 hover:text-[#dcb041] font-bold text-lg transition-colors">Inicio</a>
          <a href="/nosotros" class="mobile-link text-white/90 hover:text-[#dcb041] font-bold text-lg transition-colors">Nosotros</a>
          <a href="#cajas-especiales" class="mobile-link text-white/90 hover:text-[#dcb041] font-bold text-lg transition-colors">Colecciones</a>
          <a href="/beneficios" class="mobile-link text-white/90 hover:text-[#dcb041] font-bold text-lg transition-colors">Beneficios</a>
          <a href="/adulto-mayor" class="mobile-link text-white/90 hover:text-[#dcb041] font-bold text-lg transition-colors">Adulto Mayor</a>
        </div>
      </div>

      <div class="flex flex-col gap-4 border-t border-white/10 pt-6">
        <a
          href="https://wa.me/59179786916?text=Hola%2C%20quiero%20hacer%20un%20pedido" 
          target="_blank"
          class="w-full py-3 bg-gradient-to-r from-[#dcb041] to-[#f4c856] text-[#2A1612] text-center font-extrabold rounded-full shadow-lg transition-all"
        >
          Pedir Ahora
        </a>
      </div>
    </div>

    <!-- FONDO OSCURO DEL MENÚ MÓVIL -->
    <div
      id="drawer-overlay"
      class="fixed inset-0 z-[998] bg-black/60 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-300"
    ></div>
  </nav>

  <header
    id="inicio"
    class="relative min-h-screen overflow-hidden bg-[#14080d]"
  >
    <div
      id="hero-bg-cover"
      class="absolute inset-0 bg-no-repeat
           bg-cover bg-[position:80%_center] scale-100
           lg:bg-center lg:scale-105 transition-all duration-700"
      data-bg-cochabamba="/img/portadataboada.png"
      data-bg-lapaz="/img/portadalapaz.png"
      style="background-image:url('/img/portadataboada.png')"
    >
    </div>

    <div
      class="absolute inset-0 bg-[linear-gradient(to_right,rgba(10,5,7,0.96)_0%,rgba(20,10,15,0.92)_20%,rgba(38,18,25,0.82)_42%,rgba(62,28,40,0.58)_68%,rgba(120,70,85,0.18)_88%,rgba(255,255,255,0)_100%)]"
    >
    </div>

    <!-- BRILLO PREMIUM -->

    <div
      class="absolute top-[-180px] left-[-120px] w-[500px] h-[500px] rounded-full bg-[#f6d98d]/10 blur-[120px]"
    >
    </div>

    <!-- PARTICULAS -->

    <div class="absolute inset-0 overflow-hidden z-[2]">
      <!-- LUCES -->

      <span
        class="absolute top-[52%] left-[20%] w-2 h-2 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float"
      ></span>

      <span
        class="absolute top-[48%] left-[34%] w-1 h-1 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:1s]"
      ></span>

      <span
        class="absolute top-[58%] left-[48%] w-2 h-2 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:2s]"
      ></span>

      <span
        class="absolute top-[66%] left-[64%] w-1.5 h-1.5 rounded-full bg-[#F6D983] shadow-[0_0_20px_rgba(255,220,120,0.9)] animate-float [animation-delay:3s]"
      ></span>

      <!-- PETALOS (SVG inline) -->

      <!-- CHOCOLATE Y CACAO FALL (SVGs) -->
      <svg class="chocolate-fall absolute left-[5%] w-3 opacity-60" style="animation-duration:9s; animation-delay:2.5s;" viewBox="0 0 10 10" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="5" cy="5" r="4" fill="#3D1C06" /></svg>
      <svg class="chocolate-fall absolute left-[12%] w-7 opacity-75" style="animation-duration:14s; animation-delay:0s;" viewBox="0 0 40 60" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20,10 C32,20 32,40 20,50 C8,40 8,20 20,10 Z" fill="#3D1C06" /><path d="M20,15 C24,22 24,38 20,45" stroke="#D4AF37" stroke-width="1.5" stroke-linecap="round" opacity="0.6" /></svg>
      <svg class="chocolate-fall absolute left-[22%] w-5 opacity-60" style="animation-duration:9s; animation-delay:1s;" viewBox="0 0 30 30" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M15,5 L17,12 L24,15 L17,18 L15,25 L13,18 L6,15 L13,12 Z" fill="#2A1612" /></svg>
      <svg class="chocolate-fall absolute left-[33%] w-5 opacity-65" style="animation-duration:13s; animation-delay:6s;" viewBox="0 0 40 60" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20,15 C28,23 28,37 20,45 C12,37 12,23 20,15 Z" fill="#42200D" /></svg>
      <svg class="chocolate-fall absolute left-[45%] w-8 opacity-80" style="animation-duration:11s; animation-delay:3s;" viewBox="0 0 40 60" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20,12 C30,22 30,38 20,48 C10,38 10,22 20,12 Z" fill="#5C3821" /><path d="M20,17 C23,24 23,36 20,43" stroke="#F6D98D" stroke-width="1.2" stroke-linecap="round" opacity="0.5" /></svg>
      <svg class="chocolate-fall absolute left-[53%] w-4 opacity-60" style="animation-duration:12s; animation-delay:5s;" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><polygon points="10,3 17,7 15,15 7,17 3,10" fill="#2E160A" /></svg>
      <svg class="chocolate-fall absolute left-[62%] w-4 opacity-75" style="animation-duration:8s; animation-delay:4s;" viewBox="0 0 30 30" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M15,7 L17,12 L22,15 L17,18 L15,23 L13,18 L8,15 L13,12 Z" fill="#D4AF37" /></svg>
      <svg class="chocolate-fall absolute left-[70%] w-3 opacity-80" style="animation-duration:7s; animation-delay:0.5s;" viewBox="0 0 10 10" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="5" cy="5" r="4" fill="#D4AF37" /></svg>
      <svg class="chocolate-fall absolute left-[78%] w-6 opacity-70" style="animation-duration:16s; animation-delay:1.5s;" viewBox="0 0 40 60" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20,10 C32,20 32,40 20,50 C8,40 8,20 20,10 Z" fill="url(#goldGrad)" /><defs><linearGradient id="goldGrad" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" stop-color="#D4AF37" /><stop offset="50%" stop-color="#F6D98D" /><stop offset="100%" stop-color="#AA7C11" /></linearGradient></defs></svg>
      <svg class="chocolate-fall absolute left-[88%] w-5 opacity-70" style="animation-duration:10s; animation-delay:2s;" viewBox="0 0 30 30" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M15,5 L17,12 L24,15 L17,18 L15,25 L13,18 L6,15 L13,12 Z" fill="#7A4B29" /></svg>
      <svg class="chocolate-fall absolute left-[93%] w-2.5 opacity-75" style="animation-duration:8s; animation-delay:7s;" viewBox="0 0 10 10" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="5" cy="5" r="4" fill="#F6D98D" /></svg>
    </div>

    <!-- CONTENIDO -->

    <div
      class="relative z-[5] w-full max-w-[1440px] mx-auto min-h-screen px-6 lg:pl-16 lg:pr-10 pt-[120px] pb-20 grid lg:grid-cols-2 items-center gap-8"
    >
      <!-- TEXTO -->

      <div class="max-w-[500px]">
        <!-- MINI TEXTO -->

        <div class="flex items-center gap-4 mb-6">
          <div
            class="w-10 h-[1px] bg-gradient-to-r from-[#f6d98d] to-transparent"
          >
          </div>

          <span
            class="uppercase tracking-[0.38em] text-[#f6d98d] text-[0.72rem] font-semibold"
          >
            TRADICIÓN DESDE 1948
          </span>
        </div>

        <!-- TITULO -->

        <h1
          class="font-playfair text-[clamp(3.2rem,6.5vw,5.5rem)] leading-[0.92] tracking-[-0.06em] text-[#fff7f8] drop-shadow-[0_12px_35px_rgba(0,0,0,0.45)]"
        >
          Tradición<br />

          <span class="text-[#f6d98d] italic font-[600]"> que deleita </span>
        </h1>

        <!-- TEXTO -->

        <p
          class="mt-6 text-[clamp(0.95rem,1.5vw,1.1rem)] leading-[1.8] text-[#f3dfe5]/75 max-w-[460px] font-light"
        >
          Disfruta del auténtico chocolate boliviano con más de siete décadas de historia.
          Elaborado con la más fina selección de cacao de nuestra tierra, manteniendo 
          recetas tradicionales de generación en generación.
        </p>

        <!-- BOTONES -->

        <div class="mt-10 flex flex-wrap items-center gap-3.5">
          <a
            href="#cajas-especiales"
            class="group inline-flex items-center gap-2.5 px-7 py-4 rounded-full bg-[linear-gradient(145deg,#f6d98d,#dcb041)] text-[#2e1119] font-bold text-xs md:text-sm shadow-[0_10px_25px_rgba(220,176,65,0.2)] hover:scale-[1.03] transition-all duration-500 relative overflow-hidden"
          >
            <span class="relative z-10 tracking-wide">✦ Ver Catalogo</span>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/60 to-transparent -translate-x-[130%] group-hover:animate-shimmer"></div>
          </a>

          <a
            href="/beneficios"
            class="group inline-flex items-center gap-2.5 px-7 py-4 rounded-full border border-warm-white/35 bg-warm-white/12 text-warm-white font-bold text-xs md:text-sm hover:scale-[1.03] hover:bg-warm-white/20 transition-all duration-500 relative overflow-hidden"
          >
            <span class="relative z-10 tracking-wide">Beneficios</span>
          </a>
        </div>
      </div>
    </div>
  </header>

  <section class="relative py-20 md:py-24 bg-[#FAF5F0] overflow-hidden border-y border-[#f0e6d8]">
    <!-- Sutil brillo decorativo de fondo -->
    <div class="absolute -right-20 top-1/2 -translate-y-1/2 w-48 h-48 bg-gradient-radial from-amber-200/10 to-transparent blur-3xl pointer-events-none"></div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      
      <!-- CABECERA SUAVE: Centrada y con fondo aclarado -->
<div class="w-full max-w-4xl mx-auto mb-12 p-8 rounded-2xl text-center">
  
  <!-- Categoría Centrada -->
  <div class="flex items-center justify-center gap-2 mb-4">
    <span class="w-6 h-[2px] bg-[#8f6c49]"></span>
    <span class="text-xs uppercase tracking-widest text-[#8f6c49] font-bold font-sans">Nuestra Propuesta</span>
    <span class="w-6 h-[2px] bg-[#8f6c49]"></span>
  </div>
  
  <!-- Título Principal Centrado -->
  <h2 class="font-serif text-3xl md:text-5xl text-[#2A1612] font-extrabold leading-tight tracking-tight mb-4">
    Calidad y elegancia en cada detalle.
  </h2>
  
  <!-- Mini Texto Agregado (Ajustado para Celular) -->
  <p class="text-sm md:text-base text-[#5D4037]/80 max-w-lg mx-auto leading-relaxed">
    Descubre combinaciones únicas de sabor, texturas delicadas y la tradición que nos caracteriza en cada pieza.
  </p>

</div>

      <!-- GRILLA DE BENEFICIOS / CARACTERÍSTICAS -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 items-stretch">
        
        <!-- Tarjeta 1: Calidad Inigualable -->
        <div class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10">
          <!-- Imagen de fondo texturada -->
          <img 
            src="/img/razon2.png" 
            alt="Calidad Inigualable" 
            class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:scale-105 transition-transform duration-700 pointer-events-none" 
          />
          <!-- Overlay degradado oscuro gradual -->
          <div class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"></div>
          <!-- Overlay café en hover -->
          <div class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"></div>
          
          <!-- Contenido en la base -->
          <div class="relative z-10 flex flex-col items-start text-left text-white">
            <!-- Icono blanco -->
            <div class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500">
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="8" r="7" />
                <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88" />
              </svg>
            </div>
            
            <h3 class="font-serif font-bold text-lg md:text-xl text-white mb-2 leading-tight">Calidad Inigualable</h3>
            
            <!-- Descripción que se despliega elegantemente en hover -->
            <div class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out">
              <p class="overflow-hidden text-[11px] text-white/80 leading-relaxed font-sans transition-all duration-500">
                Cada pieza es el resultado de una selección minuciosa de los mejores granos de cacao. Garantizamos una textura excepcionalmente sedosa y un perfil de sabor profundo, diseñado para cautivar a los paladares más exigentes.
              </p>
            </div>
          </div>
        </div>

        <!-- Tarjeta 2: Maestría y Tradición -->
        <div class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10">
          <!-- Imagen de fondo texturada -->
          <img 
            src="/img/especial1.png" 
            alt="Maestría y Tradición" 
            class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:scale-105 transition-transform duration-700 pointer-events-none" 
          />
          <!-- Overlay degradado oscuro gradual -->
          <div class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"></div>
          <!-- Overlay café en hover -->
          <div class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"></div>
          
          <!-- Contenido en la base -->
          <div class="relative z-10 flex flex-col items-start text-left text-white">
            <!-- Icono blanco -->
            <div class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500">
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                <polyline points="14 2 14 8 20 8" />
                <line x1="16" y1="13" x2="8" y2="13" />
                <line x1="16" y1="17" x2="8" y2="17" />
              </svg>
            </div>
            
            <h3 class="font-serif font-bold text-lg md:text-xl text-white mb-2 leading-tight">Maestría y Tradición</h3>
            
            <!-- Descripción que se despliega elegantemente en hover -->
            <div class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out">
              <p class="overflow-hidden text-[11px] text-white/80 leading-relaxed font-sans transition-all duration-500">
                Respaldados por una profunda herencia chocolatera, fusionamos el arte de las recetas clásicas con altos estándares de elaboración. Es la verdadera esencia de la alta repostería llevada a su máxima expresión.
              </p>
            </div>
          </div>
        </div>

        <!-- Tarjeta 3: Ingredientes de Origen Noble -->
        <div class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10">
          <!-- Imagen de fondo texturada -->
          <img 
            src="/img/tableta1.png" 
            alt="Ingredientes de Origen Noble" 
            class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:scale-105 transition-transform duration-700 pointer-events-none" 
          />
          <!-- Overlay degradado oscuro gradual -->
          <div class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"></div>
          <!-- Overlay café en hover -->
          <div class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"></div>
          
          <!-- Contenido en la base -->
          <div class="relative z-10 flex flex-col items-start text-left text-white">
            <!-- Icono blanco -->
            <div class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500">
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 3.58 1 9.3a7 7 0 0 1-9 8.7z" />
                <path d="M9 22v-4" />
              </svg>
            </div>
            
            <h3 class="font-serif font-bold text-lg md:text-xl text-white mb-2 leading-tight">Origen Noble</h3>
            
            <!-- Descripción que se despliega elegantemente en hover -->
            <div class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out">
              <p class="overflow-hidden text-[11px] text-white/80 leading-relaxed font-sans transition-all duration-500">
                Privilegiamos la pureza de nuestros componentes. Al utilizar ingredientes auténticos y naturales, logramos realzar las notas aromáticas del cacao, ofreciendo una experiencia genuina y superior en cada bocado.
              </p>
            </div>
          </div>
        </div>

        <!-- Tarjeta 4: Sofisticación en Cada Detalle -->
        <div class="relative overflow-hidden rounded-[2rem] aspect-[3/4.2] group cursor-pointer shadow-lg hover:shadow-xl transition-all duration-500 flex flex-col justify-end p-6 bg-black/10">
          <!-- Imagen de fondo texturada -->
          <img 
            src="/img/razon1.png" 
            alt="Sofisticación en Cada Detalle" 
            class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:scale-105 transition-transform duration-700 pointer-events-none" 
          />
          <!-- Overlay degradado oscuro gradual -->
          <div class="absolute inset-0 bg-gradient-to-t from-[#1A0A0C]/90 via-[#1A0A0C]/35 to-transparent z-0 transition-opacity duration-500"></div>
          <!-- Overlay café en hover -->
          <div class="absolute inset-0 bg-[#2A1612] opacity-0 group-hover:opacity-85 z-0 transition-opacity duration-500"></div>
          
          <!-- Contenido en la base -->
          <div class="relative z-10 flex flex-col items-start text-left text-white">
            <!-- Icono blanco -->
            <div class="w-12 h-12 rounded-full bg-white/10 backdrop-blur-md flex items-center justify-center mb-4 text-[#f6d98d] group-hover:bg-[#8f6c49]/80 group-hover:text-white transition-all duration-500">
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M6 3h12l4 6-10 13L2 9z" />
                <path d="M11 3 8 9l4 13 4-13-3-6" />
                <path d="M2 9h20" />
              </svg>
            </div>
            
            <h3 class="font-serif font-bold text-lg md:text-xl text-white mb-2 leading-tight">Sofisticación</h3>
            
            <!-- Descripción que se despliega elegantemente en hover -->
            <div class="grid grid-rows-[0fr] group-hover:grid-rows-[1fr] transition-all duration-500 ease-in-out">
              <p class="overflow-hidden text-[11px] text-white/80 leading-relaxed font-sans transition-all duration-500">
                Más que un chocolate, ofrecemos una experiencia sensorial completa. Nuestras líneas de presentación reflejan una elegancia indiscutible, convirtiendo cada producto en una obra de arte digna de ser compartida y admirada.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SECCIÓN COMUNIDAD TABOADA (ESTILO MODERNO INSPIRADO EN DISEÑO CON FONDO1.PNG Y COLORES INSTITUCIONALES) -->
  <section class="relative py-20 md:py-28 overflow-hidden bg-[#FAF5F0] border-b border-[#e8dccb]/50 text-[#2A1612]">
    
    <!-- Imagen de fondo fondo1.png elegante -->
    <div 
      class="absolute inset-0 bg-cover bg-center opacity-20 pointer-events-none mix-blend-multiply"
      style="background-image: url('/img/fondo1.png');"
    ></div>

    <!-- Brillo dorado sutil de fondo -->
    <div class="absolute -left-1/4 top-1/4 w-[600px] h-[600px] rounded-full bg-[#8f6c49]/5 blur-[120px] pointer-events-none"></div>
    <div class="absolute -right-1/4 bottom-1/4 w-[600px] h-[600px] rounded-full bg-[#8f6c49]/5 blur-[120px] pointer-events-none"></div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-16 items-center">
        
        <!-- LADO IZQUIERDO (cols 1-5): Retrato / Foto en Marco Circular -->
        <div class="lg:col-span-5 flex justify-center relative">
          <div class="relative w-[320px] sm:w-[380px] md:w-[400px] aspect-square flex items-center justify-center">
            
            <!-- Círculo Dorado de Fondo -->
            <div class="absolute inset-4 rounded-full bg-gradient-to-tr from-[#dcb041] to-[#f6d98d] shadow-xl transform rotate-3"></div>

            <!-- Contenedor Circular con Imagen -->
            <div class="relative w-[90%] h-[90%] rounded-full overflow-hidden border-4 border-white shadow-2xl z-10 bg-[#2A1612]">
              <img 
                src="/img/comunidad_degustacion.jpg" 
                alt="Comunidad Taboada" 
                class="w-full h-full object-cover object-center transform hover:scale-105 transition-transform duration-700"
              />
            </div>

          </div>
        </div>

        <!-- LADO DERECHO (cols 6-12): Contenido e Información de Beneficios -->
        <div class="lg:col-span-7 flex flex-col items-start text-left lg:pl-4">
          
          <!-- Eyebrow -->
          <div class="flex items-center gap-2 mb-3">
            <span class="w-8 h-[2px] bg-[#8f6c49]"></span>
            <span class="text-xs md:text-sm uppercase tracking-widest text-[#8f6c49] font-extrabold font-sans">
              Programa de Lealtad
            </span>
          </div>
          
          <!-- Título Principal -->
          <h2 class="font-serif text-4xl sm:text-5xl lg:text-6xl text-[#2A1612] leading-[1.15] mb-5 font-bold tracking-tight">
            ¿Por qué unirte a <span class="text-[#8f6c49] italic font-normal">Comunidad Taboada?</span>
          </h2>
          
          <!-- Descripción -->
          <p class="text-[#5D4037] text-sm md:text-base leading-relaxed mb-6 max-w-xl font-sans">
            Sé parte de una dinastía de sabor que trasciende generaciones. Registrándote hoy en nuestra <strong class="text-[#2A1612]">Comunidad Oficial</strong>, desbloqueas privilegios únicos y experiencias pensadas para amantes del auténtico chocolate.
          </p>

          <!-- 3 BENEFICIOS CLAVE PRESENTADOS EN EL ESTILO DE MÉTRICAS (3 COLUMNAS) -->
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 md:gap-8 w-full max-w-xl mb-10 pb-8 border-b border-[#e8dccb]">
            
            <!-- Beneficio 1: Catas -->
            <div class="text-left">
              <span class="block text-2xl sm:text-3xl font-extrabold text-[#dcb041] font-serif tracking-tight mb-1">Catas VIP</span>
              <span class="text-xs font-bold text-[#2A1612] uppercase tracking-wider block mb-1">Catas de Chocolate</span>
              <p class="text-xs text-[#5D4037] leading-relaxed font-sans">Invitaciones a experiencias de maridaje y degustaciones de cacao fino.</p>
            </div>

            <!-- Beneficio 2: Descuentos -->
            <div class="text-left">
              <span class="block text-2xl sm:text-3xl font-extrabold text-[#dcb041] font-serif tracking-tight mb-1">10% Off</span>
              <span class="text-xs font-bold text-[#2A1612] uppercase tracking-wider block mb-1">Descuentos Exclusivos</span>
              <p class="text-xs text-[#5D4037] leading-relaxed font-sans">10% de descuento automático en tus compras en nuestras boutiques.</p>
            </div>

            <!-- Beneficio 3: Promociones -->
            <div class="text-left">
              <span class="block text-2xl sm:text-3xl font-extrabold text-[#dcb041] font-serif tracking-tight mb-1">Especiales</span>
              <span class="text-xs font-bold text-[#2A1612] uppercase tracking-wider block mb-1">Promociones Únicas</span>
              <p class="text-xs text-[#5D4037] leading-relaxed font-sans">Acceso a ofertas del mes, regalos de cumpleaños y colecciones privadas.</p>
            </div>

          </div>

          <!-- Botón de Acción Estilo Píldora Dual + Firma Cursiva -->
          <div class="flex flex-wrap items-center gap-6 w-full max-w-lg">
            
            <!-- Botón Píldora Dual estilo "Download CV ->" de la referencia -->
            <a
              href="/beneficios"
              class="group inline-flex items-center gap-3 p-1.5 pr-6 rounded-full bg-[#2A1612] hover:bg-[#8f6c49] text-[#FAF5F0] transition-all duration-300 shadow-xl hover:shadow-2xl"
            >
              <div class="w-10 h-10 rounded-full bg-[#dcb041] text-[#2A1612] flex items-center justify-center font-bold transition-transform duration-300 group-hover:scale-110">
                <svg class="w-5 h-5 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path>
                </svg>
              </div>
              <span class="font-extrabold text-xs md:text-sm tracking-wider uppercase pl-1">
                Unirse a la Comunidad
              </span>
            </a>

            <!-- Firma / Sello en Fuente Cursiva -->
            <div class="font-cursiva text-2xl md:text-3xl text-[#8f6c49] font-normal tracking-wide">
              Familia Taboada
            </div>

          </div>

        </div>
        
      </div>
    </div>
  </section>
<section
    id="elegancia"
    class="relative bg-gradient-to-br from-[#FAF5F0] via-[#F3ECE3] to-[#FAF5F0] py-16 md:py-24 border-t border-[#e8dccb]/50 overflow-hidden"
  >
    <div class="max-w-6xl mx-auto px-5 relative z-10">
      <!-- TITULO -->
      <div class="text-center mb-12">
        <h2
          class="text-3xl md:text-5xl font-elegante text-[#2A1612] mb-2 tracking-tight font-bold"
        >
          Dilo con un detalle inolvidable
        </h2>

        <p
          class="font-cursiva text-xl md:text-3xl text-[#8f6c49] tracking-wide"
        >
          Porque las palabras sobran cuando el sabor habla por ti.
        </p>
      </div>

      <!-- CARDS -->
      <div class="flex flex-col gap-6 max-w-5xl mx-auto">
        <!-- CARD 1 -->
        <div
          class="bg-[#fbf8f3]/95 rounded-[2rem] border border-[#e8dccb] shadow-md hover:-translate-y-1 hover:shadow-xl hover:shadow-amber-900/5 transition-all duration-500 flex flex-col md:flex-row overflow-hidden group"
        >
          <!-- IMAGEN -->
          <div
            class="md:w-[42%] p-6 md:p-7 flex items-center justify-center relative overflow-hidden"
          >
            <div
              class="absolute w-52 h-52 bg-[#f6d98d]/10 rounded-full blur-3xl opacity-70"
            >
            </div>

            <img
              src="/img/especial1.png"
              alt="Estuche Selección Premium"
              class="object-contain w-full h-full max-h-56 relative z-10 drop-shadow-[0_10px_18px_rgba(0,0,0,0.15)] group-hover:scale-105 transition-transform duration-500"
            />
          </div>

          <!-- TEXTO -->
          <div class="md:w-[58%] p-6 md:p-8 flex flex-col justify-center">
            <h3
              class="text-4xl md:text-5xl font-elegante font-bold text-[#2A1612] mb-3"
            >
              Bs. 72
            </h3>

            <h4
              class="text-xl md:text-2xl font-bold text-[#8f6c49] mb-2 font-elegante"
            >
              Estuche Selección Premium

              <span class="font-normal text-[#5D4037]/80 text-base font-sans">
                250g
              </span>
            </h4>

            <p class="text-[#5D4037]/80 italic text-sm mb-3">
              "Un deleite para compartir"
            </p>

            <p class="text-[#5D4037]/90 text-sm leading-relaxed mb-6 font-sans">
              Más que chocolate... una tradición de sabor único y elegancia.
            </p>

            <div class="flex flex-wrap items-center justify-between gap-4 mt-auto pt-4 border-t border-[#e8dccb]/70">
              <span
                class="bg-[#FAF5F0] border border-[#e8dccb] text-[#8f6c49] text-[11px] font-bold px-4 py-2 rounded-full font-sans"
              >
                Detalle Premium
              </span>

              <div class="flex items-center gap-2">
                <!-- Añadir al carrito (icono bag) -->
                <button
                  type="button"
                  class="add-to-cart-btn w-9 h-9 bg-[#FAF5F0] hover:bg-[#2A1612] hover:text-[#dcb041] border border-[#2A1612]/20 text-[#2A1612] rounded-full flex items-center justify-center transition-all duration-300 shadow-sm cursor-pointer"
                  title="Añadir al carrito"
                  data-name="Estuche Selección Premium"
                  data-size="250g"
                  data-img="/img/especial1.png"
                  data-price-cochabamba="BS. 72"
                  data-price-lapaz="BS. 72"
                >
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" />
                  </svg>
                </button>

                <!-- Comprar directamente -->
                <button
                  type="button"
                  class="direct-buy-btn bg-[#f6d98d] hover:bg-[#f0cb69] text-[#3a1823] text-xs font-black px-5 py-2.5 rounded-full transition-all duration-300 shadow-md text-center cursor-pointer"
                  data-name="Estuche Selección Premium"
                  data-size="250g"
                  data-price-cochabamba="BS. 72"
                  data-price-lapaz="BS. 72"
                >
                  Comprar
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- CARD 2 -->
        <div
          class="bg-[#fbf8f3]/95 rounded-[2rem] border border-[#e8dccb] shadow-md hover:-translate-y-1 hover:shadow-xl hover:shadow-amber-900/5 transition-all duration-500 flex flex-col md:flex-row-reverse overflow-hidden group"
        >
          <!-- IMAGEN -->
          <div
            class="md:w-[42%] p-6 md:p-7 md:pr-24 flex items-center justify-center relative overflow-hidden"
          >
            <div
              class="absolute w-52 h-52 bg-[#f6d98d]/10 rounded-full blur-3xl opacity-70"
            >
            </div>

            <img
              src="/img/prom2.png"
              alt="Caja Cartón Corazón"
              class="object-contain w-full h-full max-h-56 relative z-10 drop-shadow-[0_10px_18px_rgba(0,0,0,0.15)] group-hover:scale-105 transition-transform duration-500"
            />
          </div>

          <!-- TEXTO -->
          <div class="md:w-[58%] p-6 md:p-8 md:px-24 flex flex-col justify-center">
            <h3
              class="text-4xl md:text-5xl font-elegante font-bold text-[#2A1612] mb-3"
            >
              Bs. 21.5
            </h3>

            <h4
              class="text-xl md:text-2xl font-bold text-[#8f6c49] mb-2 font-elegante"
            >
              Caja Cartera

              <span class="font-normal text-[#5D4037]/80 text-base font-sans">
                100g
              </span>
            </h4>

            <p class="text-[#5D4037]/80 italic text-sm mb-3">
              "Tradición en formato unique"
            </p>

            <p class="text-[#5D4037]/90 text-sm leading-relaxed mb-6 font-sans">
              Un detalle delicado, elegante y perfecto para cualquier ocasión especial.
            </p>

            <div class="flex flex-wrap items-center justify-between gap-4 mt-auto pt-4 border-t border-[#e8dccb]/70">
              <span
                class="bg-[#FAF5F0] border border-[#e8dccb] text-[#8f6c49] text-[11px] font-bold px-4 py-2 rounded-full font-sans"
              >
                Exclusivo
              </span>

              <div class="flex items-center gap-2">
                <!-- Añadir al carrito (icono bag) -->
                <button
                  type="button"
                  class="add-to-cart-btn w-9 h-9 bg-[#FAF5F0] hover:bg-[#2A1612] hover:text-[#dcb041] border border-[#2A1612]/20 text-[#2A1612] rounded-full flex items-center justify-center transition-all duration-300 shadow-sm cursor-pointer"
                  title="Añadir al carrito"
                  data-name="Caja Cartera "
                  data-size=" 100g"
                  data-img="/img/prom2.png"
                  data-price-cochabamba="BS. 21.5"
                  data-price-lapaz="BS. 21.5"
                >
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" />
                  </svg>
                </button>

                <!-- Comprar directamente -->
                <button
                  type="button"
                  class="direct-buy-btn bg-[#f6d98d] hover:bg-[#f0cb69] text-[#3a1823] text-xs font-black px-5 py-2.5 rounded-full transition-all duration-300 shadow-md text-center cursor-pointer"
                  data-name="Caja Cartera "
                  data-size="100g"
                  data-price-cochabamba="BS. 21.5"
                  data-price-lapaz="BS. 21.5"
                >
                  Comprar
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <section
    id="cajas-especiales"
    class="relative bg-gradient-to-br from-[#FAF5F0] via-[#F3ECE3] to-[#FAF5F0] py-16 md:py-24 border-t border-[#e8dccb]/50 overflow-hidden"
  >
    <div
      id="cajas-especiales-bg"
      class="absolute inset-0 bg-gradient-to-br from-[#FAF5F0] via-[#F3ECE3] to-[#FAF5F0] transition-all duration-500"
    >
    </div>

    <div
      class="absolute top-0 right-0 w-96 h-96 bg-gradient-radial from-amber-100/20 to-transparent blur-[100px] opacity-60"
    >
    </div>

    <div class="relative z-10 max-w-7xl mx-auto px-6">
      <div class="mb-10 md:mb-16">
        <div class="flex justify-start mb-8 md:mb-10">
          <h2
            class="text-3xl md:text-5xl font-elegante font-bold text-[#2A1612] relative inline-block tracking-tight"
          >
            Colecciones

            <!-- línea elegante -->
            <span
              class="absolute left-0 -bottom-3 w-28 h-[4px] bg-[#dcb041] rounded-full shadow-[0_0_10px_rgba(220,176,65,0.35)]"
            ></span>
          </h2>
        </div>

        <!-- FILTROS DE CATEGORÍAS -->
        <div class="mb-12 overflow-x-auto whitespace-nowrap pb-4 scrollbar-none flex gap-2 md:gap-3 max-w-full">
          {categories.map((cat) => (
            <button
              type="button"
              class={`cat-btn px-5 py-2.5 rounded-full text-xs md:text-sm font-semibold transition-all duration-300 cursor-pointer
                ${cat.id === 'todo' 
                  ? 'bg-[#dcb041] text-[#2e1119] shadow-[0_6px_18px_rgba(220,176,65,0.25)]' 
                  : 'bg-[#fbf8f3] border border-[#e8dccb] text-[#2A1612] hover:bg-[#FAF0E6]'
                }`}
              data-target={cat.id}
            >
              {cat.label}
            </button>
          ))}
        </div>

        <!-- GRID DE PRODUCTOS -->
        <div id="product-grid" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 lg:gap-8 min-h-[450px]">
          {products.map((product) => (
            <div
              class="product-card group relative bg-[#fbf8f3]/95 rounded-[2rem] p-6 border border-[#e8dccb] flex flex-col overflow-hidden transition-all duration-500 hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(251,207,232,0.4)]"
              data-categories={product.category}
              data-available={product.available.join(" ")}
              data-price-cochabamba={product.price.cochabamba}
              data-price-lapaz={product.price["la-paz"]}
            >
              <!-- Botón Añadir al Carrito (Flotante arriba a la derecha de la tarjeta con Tooltip) -->
              <div class="absolute top-5 right-5 z-20 group/tooltip">
                <button
                  type="button"
                  class="add-to-cart-btn w-10 h-10 bg-white/90 hover:bg-[#2A1612] hover:text-[#dcb041] border border-[#2A1612]/10 text-[#2A1612] rounded-full flex items-center justify-center transition-all duration-300 shadow-md hover:scale-105 active:scale-95 cursor-pointer"
                  data-name={product.name}
                  data-size={product.size}
                  data-img={product.img}
                  data-price-cochabamba={product.price.cochabamba}
                  data-price-lapaz={product.price["la-paz"]}
                >
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" />
                  </svg>
                </button>
                <!-- Tooltip personalizado -->
                <span class="absolute right-0 -bottom-9 bg-[#2A1612] text-white text-[10px] font-bold px-2.5 py-1 rounded-md shadow-lg whitespace-nowrap opacity-0 pointer-events-none group-hover/tooltip:opacity-100 transition-opacity duration-300 z-30 border border-white/10">
                  Añadir al carrito
                </span>
              </div>

              <!-- IMAGEN -->
              <div class="w-full h-56 mb-6 flex items-center justify-center relative z-10">
                <img
                  src={product.img}
                  alt={product.name}
                  class="object-contain max-h-full drop-shadow-xl group-hover:scale-110 transition-transform duration-500 relative z-10"
                />
              </div>

              <!-- CONTENIDO -->
              <div class="relative z-10 flex-grow flex flex-col">
                <h3 class="text-xl font-serif text-[#2A1612] font-bold leading-tight mb-1">
                  {product.name} <br />
                  <span class="text-base font-normal">{product.size}</span>
                </h3>
                <p class="text-[11px] text-[#8f6c49]/80 leading-relaxed mb-6 flex-grow font-sans">
                  {product.desc}
                </p>
              </div>

              <!-- PRECIO Y COMPRA -->
              <div class="relative z-10 flex items-end justify-between mt-auto pt-4 border-t border-[#e8dccb]/70">
                <div>
                  <span class="block text-[9px] text-[#8f6c49] uppercase mb-0.5 tracking-wider font-sans">Precio</span>
                  <p class="product-price-label text-xl font-serif font-bold text-[#2A1612]">{product.price.cochabamba}</p>
                </div>

                <!-- Comprar directamente -->
                <button
                  type="button"
                  class="direct-buy-btn bg-[#f6d98d] hover:bg-[#f0cb69] text-[#3a1823] text-xs font-black px-5 py-2.5 rounded-full transition-all duration-300 shadow-sm text-center cursor-pointer"
                  data-name={product.name}
                  data-size={product.size}
                  data-price-cochabamba={product.price.cochabamba}
                  data-price-lapaz={product.price["la-paz"]}
                >
                  Comprar
                </button>
              </div>
            </div>
          ))}

          <!-- MENSAJE DE RESULTADO VACÍO -->
          <div id="no-products-msg" class="hidden col-span-full py-20 text-center">
            <p class="text-[#8f6c49] text-lg font-medium font-sans">No hay productos en esta categoría por el momento.</p>
          </div>
        </div>

        <!-- BOTÓN VER MÁS / VER MENOS -->
        <div id="see-more-container" class="flex justify-end mt-12 hidden">
          <button
            id="see-more-btn"
            type="button"
            class="px-8 py-3.5 rounded-full border border-[#2A1612] text-[#2A1612] font-semibold text-sm hover:bg-[#2A1612] hover:text-white transition-all duration-300 shadow-md cursor-pointer font-sans"
          >
            Ver más
          </button>
        </div>
      </div>
    </div>
  </section>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // Selectores de Categorías
      const catButtons = document.querySelectorAll('.cat-btn');
      const cards = document.querySelectorAll('.product-card');
      const noProductsMsg = document.getElementById('no-products-msg');

      // Selectores de Ubicación
      const citySelectorBtn = document.getElementById('city-selector-btn');
      const cityDropdown = document.getElementById('city-dropdown');
      const activeCityName = document.getElementById('active-city-name');
      const cityOptButtons = document.querySelectorAll('.city-opt-btn');
      const bgOverlay = document.getElementById('cajas-especiales-bg');
      const heroBgCover = document.getElementById('hero-bg-cover');

      // Selectores de Ver Más
      const seeMoreContainer = document.getElementById('see-more-container');
      const seeMoreBtn = document.getElementById('see-more-btn');

      // Estados actuales
      let currentCategory = 'todo';
      let currentCity = 'cochabamba'; // Valor inicial / fallback
      let isExpanded = false; // Estado del botón Ver Más

      // ── DROPDOWN SELECTOR DE UBICACIÓN ──
      citySelectorBtn?.addEventListener('click', (e) => {
        e.stopPropagation();
        cityDropdown?.classList.toggle('hidden');
      });

      document.addEventListener('click', () => {
        cityDropdown?.classList.add('hidden');
      });

      // ── MENÚ MÓVIL (DRAWER) ──
      const mobileMenuBtn = document.getElementById('mobile-menu-btn');
      const mobileCloseBtn = document.getElementById('mobile-close-btn');
      const mobileDrawer = document.getElementById('mobile-drawer');
      const drawerOverlay = document.getElementById('drawer-overlay');
      const mobileLinks = document.querySelectorAll('.mobile-link');

      function openDrawer() {
        mobileDrawer?.classList.remove('translate-x-full');
        drawerOverlay?.classList.remove('opacity-0', 'pointer-events-none');
      }

      function closeDrawer() {
        mobileDrawer?.classList.add('translate-x-full');
        drawerOverlay?.classList.add('opacity-0', 'pointer-events-none');
      }

      mobileMenuBtn?.addEventListener('click', (e) => {
        e.stopPropagation();
        openDrawer();
      });

      mobileCloseBtn?.addEventListener('click', closeDrawer);
      drawerOverlay?.addEventListener('click', closeDrawer);

      mobileLinks.forEach((link) => {
        link.addEventListener('click', closeDrawer);
      });

      // ── ACTUALIZAR CIUDAD SELECCIONADA ──
      function updateCity(city: string) {
        currentCity = city;
        localStorage.setItem('selected_city', city);

        // Actualizar etiqueta del botón
        if (activeCityName) {
          activeCityName.textContent = city === 'cochabamba' ? 'Cochabamba' : 'La Paz';
        }

        // Inyectar clase al document.body (Para manejo CSS global si es necesario)
        document.body.classList.remove('city-cochabamba', 'city-lapaz', 'city-la-paz');
        document.body.classList.add(`city-${city}`);

        // Actualizar la portada del Hero con una transición (Fade Out / Fade In)
        if (heroBgCover) {
          const bgCbba = heroBgCover.getAttribute('data-bg-cochabamba') || '/img/portadataboada.png';
          const bgLpz = heroBgCover.getAttribute('data-bg-lapaz') || '/img/portadalapaz.png';
          
          heroBgCover.style.opacity = '0'; // Disparar fade-out
          
          setTimeout(() => {
            if (city === 'cochabamba') {
              heroBgCover.style.backgroundImage = `url('${bgCbba}')`;
            } else if (city === 'la-paz') {
              heroBgCover.style.backgroundImage = `url('${bgLpz}')`;
            }
            heroBgCover.style.opacity = '1'; // Disparar fade-in tras aplicar nueva imagen
          }, 400); // 400ms para sincronizarse con la transición CSS
        }

        // Actualizar el degradado de fondo de Colecciones
        if (bgOverlay) {
          // Remover clases viejas de gradientes
          bgOverlay.classList.remove(
            'from-[#FAF5F0]', 'via-[#F3ECE3]', 'to-[#FAF5F0]',
            'from-[#FAF6F2]', 'via-[#EADCD0]', 'to-[#FAF6F2]',
            'from-[#FAF7F5]', 'via-[#E2DFD8]', 'to-[#FAF7F5]'
          );
          
          // Añadir clases nuevas basadas en la ciudad y aplicar transición
          bgOverlay.style.transition = 'all 0.6s ease-in-out';
          if (city === 'cochabamba') {
            bgOverlay.classList.add('from-[#FAF6F2]', 'via-[#EADCD0]', 'to-[#FAF6F2]');
          } else if (city === 'la-paz') {
            bgOverlay.classList.add('from-[#FAF7F5]', 'via-[#E2DFD8]', 'to-[#FAF7F5]');
          }
        }

        // Refrescar el filtrado de productos y los precios
        filterProducts();
      }

      // ── FILTRAR PRODUCTOS Y PRECIOS ──
      function filterProducts() {
        let matchingCards: Element[] = [];

        // 1. Clasificar tarjetas que coinciden con los filtros activos
        cards.forEach((card) => {
          const categoriesAttr = card.getAttribute('data-categories') || '';
          const categoriesList = categoriesAttr.split(' ');
          const availableAttr = card.getAttribute('data-available') || '';
          const availableList = availableAttr.split(' ');

          const matchesCategory = currentCategory === 'todo' || categoriesList.includes(currentCategory);
          const matchesCity = availableList.includes(currentCity);

          // Actualizar el precio dinámicamente antes de mostrar/ocultar
          const priceLabel = card.querySelector('.product-price-label');
          if (priceLabel) {
            const priceKey = currentCity === 'cochabamba' ? 'data-price-cochabamba' : 'data-price-lapaz';
            priceLabel.textContent = card.getAttribute(priceKey) || '';
          }

          if (matchesCategory && matchesCity) {
            matchingCards.push(card);
          } else {
            card.classList.add('hidden');
          }
        });

        // 2. Gestionar la visualización del botón "Ver más"
        const limit = 4;
        if (matchingCards.length > limit) {
          seeMoreContainer?.classList.remove('hidden');
          if (seeMoreBtn) {
            seeMoreBtn.textContent = isExpanded ? 'Ver menos' : 'Ver más';
          }

          matchingCards.forEach((card, index) => {
            if (isExpanded || index < limit) {
              card.classList.remove('hidden');
              // Pequeña animación de entrada
              (card as HTMLElement).style.opacity = '0';
              setTimeout(() => {
                (card as HTMLElement).style.opacity = '1';
                (card as HTMLElement).style.transition = 'opacity 0.4s ease, transform 0.4s ease';
              }, 30);
            } else {
              card.classList.add('hidden');
            }
          });
        } else {
          // Ocultar botón si hay 4 o menos productos y mostrarlos todos
          seeMoreContainer?.classList.add('hidden');
          matchingCards.forEach((card) => {
            card.classList.remove('hidden');
            (card as HTMLElement).style.opacity = '0';
            setTimeout(() => {
              (card as HTMLElement).style.opacity = '1';
              (card as HTMLElement).style.transition = 'opacity 0.4s ease, transform 0.4s ease';
            }, 30);
          });
        }

        // Mostrar u ocultar mensaje vacío
        if (matchingCards.length === 0) {
          noProductsMsg?.classList.remove('hidden');
        } else {
          noProductsMsg?.classList.add('hidden');
        }
      }

      // Clic en Ver Más / Ver Menos
      seeMoreBtn?.addEventListener('click', () => {
        isExpanded = !isExpanded;
        filterProducts();
      });

      // Clics en opciones de ciudades
      cityOptButtons.forEach((btn) => {
        btn.addEventListener('click', () => {
          const selected = btn.getAttribute('data-city') || 'cochabamba';
          isExpanded = false; // Resetear expansión
          updateCity(selected);
        });
      });

      // Clics en botones de categorías
      catButtons.forEach((btn) => {
        btn.addEventListener('click', () => {
          currentCategory = btn.getAttribute('data-target') || 'todo';
          isExpanded = false; // Resetear expansión

          // Actualizar estilos de los botones de categoría
          catButtons.forEach((b) => {
            b.classList.remove('bg-[#dcb041]', 'text-[#2e1119]', 'shadow-[0_6px_18px_rgba(220,176,65,0.25)]');
            b.classList.add('bg-[#fbf8f3]', 'border', 'border-[#e8dccb]', 'text-[#2A1612]', 'hover:bg-[#FAF0E6]');
          });

          btn.classList.add('bg-[#dcb041]', 'text-[#2e1119]', 'shadow-[0_6px_18px_rgba(220,176,65,0.25)]');
          btn.classList.remove('bg-[#fbf8f3]', 'border', 'border-[#e8dccb]', 'text-[#2A1612]', 'hover:bg-[#FAF0E6]');

          filterProducts();
        });
      });

      // ── INICIALIZACIÓN Y DETECCIÓN POR IP SILENCIOSA ──
      const savedCity = localStorage.getItem('selected_city');
      if (savedCity && (savedCity === 'cochabamba' || savedCity === 'la-paz')) {
        updateCity(savedCity);
      } else {
        // Petición silenciosa asíncrona a ipapi.co/json/ (HTTPS)
        fetch('https://ipapi.co/json/')
          .then((res) => {
            if (res.ok) return res.json();
            throw new Error('API Response Error');
          })
          .then((data) => {
            console.log('Ciudad detectada por API:', data.city);
            
            const cityValue = (data.city || '').toLowerCase();
            const regionValue = (data.region || '').toLowerCase();
            
            // Evaluación estricta única y exclusivamente de los campos city y region (ignora timezone)
            const detected = (cityValue.includes('paz') || regionValue.includes('paz')) ? 'la-paz' : 'cochabamba';
            updateCity(detected);
          })
          .catch((err) => {
            // Manejo silencioso de errores para evitar romper la carga (AdBlockers o fallos de red)
            updateCity('cochabamba');
          });
      }

      // ── DETECTAR Y FILTRAR POR PARÁMETROS URL (?cat=...) ──
      function checkUrlParams() {
        const urlParams = new URLSearchParams(window.location.search);
        const catParam = urlParams.get('cat');
        if (catParam) {
          const targetBtn = document.querySelector(`.cat-btn[data-target="${catParam}"]`) as HTMLButtonElement;
          if (targetBtn) {
            setTimeout(() => {
              targetBtn.click();
              const targetSection = document.getElementById('cajas-especiales');
              if (targetSection) {
                targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
              }
            }, 300);
          }
        }
      }

      checkUrlParams();

      // Interceptar clicks en enlaces del menu dropdown cuando estamos en el home
      const dropdownLinks = document.querySelectorAll('a[href*="?cat="]');
      dropdownLinks.forEach(link => {
        link.addEventListener('click', (e) => {
          const hrefAttr = link.getAttribute('href') || '';
          const isHomePage = window.location.pathname === '/' || window.location.pathname === '/index.html' || window.location.pathname === '';
          if (isHomePage) {
            const url = new URL(hrefAttr, window.location.href);
            const cat = url.searchParams.get('cat');
            if (cat) {
              const targetBtn = document.querySelector(`.cat-btn[data-target="${cat}"]`) as HTMLButtonElement;
              if (targetBtn) {
                e.preventDefault();
                targetBtn.click();
                const targetSection = document.getElementById('cajas-especiales');
                if (targetSection) {
                  targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
                // Limpiar la query de la URL sin recargar
                window.history.pushState({}, '', window.location.pathname);
              }
            }
          }
        });
      });

      // ── LOGICA COMPLETA DEL CARRITO DE COMPRAS ──
      let cart = JSON.parse(localStorage.getItem('taboada_cart') || '[]');
      
      // Selectores Modal de Cantidad
      const qtyModal = document.getElementById('quantity-modal');
      const qtyModalOverlay = document.getElementById('quantity-modal-overlay');
      const qtyModalClose = document.getElementById('quantity-modal-close');
      const modalProdName = document.getElementById('modal-product-name');
      const modalProdSize = document.getElementById('modal-product-size');
      const modalProdImg = document.getElementById('modal-product-img') as HTMLImageElement;
      const modalQtyVal = document.getElementById('modal-qty-val');
      const modalQtyMinus = document.getElementById('modal-qty-minus');
      const modalQtyPlus = document.getElementById('modal-qty-plus');
      const modalTotalPrice = document.getElementById('modal-total-price');
      const modalAddBtn = document.getElementById('modal-add-btn');

      // Selectores Drawer Carrito
      const cartDrawer = document.getElementById('cart-drawer');
      const cartOverlay = document.getElementById('cart-overlay');
      const cartDrawerClose = document.getElementById('cart-drawer-close');
      const floatingCartBtn = document.getElementById('floating-cart-btn');
      const cartCountBadge = document.getElementById('cart-count-badge');
      const cartItemsList = document.getElementById('cart-items-list');
      const cartEmptyMsg = document.getElementById('cart-empty-msg');
      const cartTotalPrice = document.getElementById('cart-total-price');
      const cartCheckoutBtn = document.getElementById('cart-checkout-btn');

      // Variables de control del Modal
      let modalActiveProduct = {
        name: '',
        size: '',
        img: '',
        price: 0,
        quantity: 1
      };

      // Limpiar texto de precio "BS. 77" -> 77
      function parsePrice(priceStr: string): number {
        // Eliminar primero la palabra "BS." o "Bs." para evitar que el punto de la abreviación se tome como decimal (.81)
        const cleanStr = priceStr.replace(/BS\./i, '').replace(/Bs\./i, '').replace(/BS/i, '').replace(/Bs/i, '');
        return parseFloat(cleanStr.replace(/[^\d.]/g, '')) || 0;
      }

      // Guardar y Actualizar
      function saveCart() {
        localStorage.setItem('taboada_cart', JSON.stringify(cart));
        updateCartUI();
      }

      function updateCartUI() {
        const totalItems = cart.reduce((acc: number, item: any) => acc + item.quantity, 0);
        if (cartCountBadge) {
          cartCountBadge.textContent = totalItems.toString();
        }

        // Renderizar items del drawer
        if (cartItemsList) {
          const itemsElements = cartItemsList.querySelectorAll('.cart-item-row');
          itemsElements.forEach(el => el.remove());

          if (cart.length === 0) {
            cartEmptyMsg?.classList.remove('hidden');
            if (cartTotalPrice) cartTotalPrice.textContent = "BS. 0";
          } else {
            cartEmptyMsg?.classList.add('hidden');
            let grandTotal = 0;

            cart.forEach((item: any, index: number) => {
              const itemTotal = item.price * item.quantity;
              grandTotal += itemTotal;

              const itemRow = document.createElement('div');
              itemRow.className = 'cart-item-row flex items-center justify-between gap-3 p-3 bg-white/5 rounded-2xl border border-white/5';
              itemRow.innerHTML = `
                <div class="flex items-center gap-3 flex-grow">
                  <img src="${item.img}" alt="${item.name}" class="w-12 h-12 object-contain bg-white/5 rounded-lg p-1" />
                  <div class="text-left">
                    <span class="block text-xs font-bold text-white line-clamp-1">${item.name}</span>
                    <span class="block text-[9px] text-[#f6d98d] font-bold">${item.size}</span>
                    <span class="block text-[10px] text-white/60 mt-0.5">BS. ${item.price} c/u</span>
                  </div>
                </div>
                <div class="flex flex-col items-end justify-between gap-1 flex-shrink-0">
                  <button class="cart-remove-item text-white/40 hover:text-red-400 text-xs font-bold p-1 cursor-pointer transition-colors" data-index="${index}">✕</button>
                  <div class="flex items-center gap-2 mt-1">
                    <button class="cart-qty-dec w-6 h-6 rounded-full bg-white/10 hover:bg-white/20 text-white flex items-center justify-center text-xs font-bold cursor-pointer" data-index="${index}">-</button>
                    <span class="text-xs font-black text-white w-4 text-center">${item.quantity}</span>
                    <button class="cart-qty-inc w-6 h-6 rounded-full bg-white/10 hover:bg-white/20 text-white flex items-center justify-center text-xs font-bold cursor-pointer" data-index="${index}">+</button>
                  </div>
                </div>
              `;
              cartItemsList.appendChild(itemRow);
            });

            if (cartTotalPrice) {
              cartTotalPrice.textContent = `BS. ${grandTotal.toFixed(1)}`;
            }
          }
        }

        bindCartDrawerEvents();
      }

      function bindCartDrawerEvents() {
        const removeButtons = document.querySelectorAll('.cart-remove-item');
        removeButtons.forEach(btn => {
          btn.addEventListener('click', (e) => {
            const idx = parseInt((e.currentTarget as HTMLButtonElement).getAttribute('data-index') || '0', 10);
            cart.splice(idx, 1);
            saveCart();
          });
        });

        const decButtons = document.querySelectorAll('.cart-qty-dec');
        decButtons.forEach(btn => {
          btn.addEventListener('click', (e) => {
            const idx = parseInt((e.currentTarget as HTMLButtonElement).getAttribute('data-index') || '0', 10);
            if (cart[idx].quantity > 1) {
              cart[idx].quantity -= 1;
            } else {
              cart.splice(idx, 1);
            }
            saveCart();
          });
        });

        const incButtons = document.querySelectorAll('.cart-qty-inc');
        incButtons.forEach(btn => {
          btn.addEventListener('click', (e) => {
            const idx = parseInt((e.currentTarget as HTMLButtonElement).getAttribute('data-index') || '0', 10);
            cart[idx].quantity += 1;
            saveCart();
          });
        });
      }

      function openCartDrawer() {
        cartDrawer?.classList.remove('translate-x-full');
        cartOverlay?.classList.remove('hidden');
      }

      function closeCartDrawer() {
        cartDrawer?.classList.add('translate-x-full');
        cartOverlay?.classList.add('hidden');
      }

      function openQtyModal(name: string, size: string, img: string, price: number) {
        modalActiveProduct = { name, size, img, price, quantity: 1 };
        
        if (modalProdName) modalProdName.textContent = name;
        if (modalProdSize) modalProdSize.textContent = size;
        if (modalProdImg) modalProdImg.src = img;
        if (modalQtyVal) modalQtyVal.textContent = "1";
        
        qtyModalOverlay?.classList.remove('hidden');
        qtyModal?.classList.remove('hidden');
        
        setTimeout(() => {
          qtyModal?.classList.remove('scale-95', 'opacity-0');
          qtyModal?.classList.add('scale-100', 'opacity-100');
        }, 10);
      }

      function closeQtyModal() {
        qtyModal?.classList.remove('scale-100', 'opacity-100');
        qtyModal?.classList.add('scale-95', 'opacity-0');
        
        setTimeout(() => {
          qtyModal?.classList.add('hidden');
          qtyModalOverlay?.classList.add('hidden');
        }, 300);
      }


      // Enlaces de eventos principales
      const buyButtons = document.querySelectorAll('.add-to-cart-btn');
      buyButtons.forEach(btn => {
        btn.addEventListener('click', (e) => {
          const target = e.currentTarget as HTMLButtonElement;
          const name = target.getAttribute('data-name') || '';
          const size = target.getAttribute('data-size') || '';
          const img = target.getAttribute('data-img') || '';
          
          const priceKey = currentCity === 'cochabamba' ? 'data-price-cochabamba' : 'data-price-lapaz';
          const priceStr = target.getAttribute(priceKey) || target.getAttribute('data-price-cochabamba') || '0';
          const price = parsePrice(priceStr);

          openQtyModal(name, size, img, price);
        });
      });

      // Compra directa de un solo producto
      const directBuyButtons = document.querySelectorAll('.direct-buy-btn');
      directBuyButtons.forEach(btn => {
        btn.addEventListener('click', (e) => {
          const target = e.currentTarget as HTMLButtonElement;
          const name = target.getAttribute('data-name') || '';
          const size = target.getAttribute('data-size') || '';
          
          const priceKey = currentCity === 'cochabamba' ? 'data-price-cochabamba' : 'data-price-lapaz';
          const priceStr = target.getAttribute(priceKey) || target.getAttribute('data-price-cochabamba') || '';
          const priceVal = parsePrice(priceStr);
          
          const cityLabel = currentCity === 'cochabamba' ? 'Cochabamba' : 'La Paz';
          
          let message = `¡Hola! Quisiera comprar el siguiente producto de Chocolates Taboada:\n\n`;
          message += `Ciudad de entrega: ${cityLabel}\n`;
          message += `------------------------------------------\n`;
          message += `• 1 x ${name} (${size})\n`;
          message += `  Precio unitario: BS. ${priceVal.toFixed(1)}\n`;
          message += `  Subtotal: BS. ${priceVal.toFixed(1)}\n`;
          message += `------------------------------------------\n`;
          message += `Total a pagar: BS. ${priceVal.toFixed(1)}\n\n`;
          message += `¡Muchas gracias!`;
          
          const waNumber = "59179786916";
          const encodedMsg = encodeURIComponent(message);
          const waLink = `https://wa.me/${waNumber}?text=${encodedMsg}`;
          
          window.open(waLink, '_blank');
        });
      });

      modalQtyMinus?.addEventListener('click', () => {
        if (modalActiveProduct.quantity > 1) {
          modalActiveProduct.quantity -= 1;
          if (modalQtyVal) modalQtyVal.textContent = modalActiveProduct.quantity.toString();
        }
      });

      modalQtyPlus?.addEventListener('click', () => {
        modalActiveProduct.quantity += 1;
        if (modalQtyVal) modalQtyVal.textContent = modalActiveProduct.quantity.toString();
      });

      modalAddBtn?.addEventListener('click', () => {
        const existingIdx = cart.findIndex((item: any) => item.name === modalActiveProduct.name && item.size === modalActiveProduct.size);
        if (existingIdx > -1) {
          cart[existingIdx].quantity += modalActiveProduct.quantity;
        } else {
          cart.push({
            name: modalActiveProduct.name,
            size: modalActiveProduct.size,
            img: modalActiveProduct.img,
            price: modalActiveProduct.price,
            quantity: modalActiveProduct.quantity
          });
        }
        
        saveCart();
        closeQtyModal();
        
        setTimeout(() => {
          openCartDrawer();
        }, 400);
      });

      qtyModalClose?.addEventListener('click', closeQtyModal);
      qtyModalOverlay?.addEventListener('click', closeQtyModal);

      floatingCartBtn?.addEventListener('click', openCartDrawer);
      cartDrawerClose?.addEventListener('click', closeCartDrawer);
      cartOverlay?.addEventListener('click', closeCartDrawer);

      cartCheckoutBtn?.addEventListener('click', () => {
        if (cart.length === 0) return;

        const cityLabel = currentCity === 'cochabamba' ? 'Cochabamba' : 'La Paz';
        let message = `¡Hola! Quisiera realizar el siguiente pedido de Chocolates Taboada:\n\n`;
        message += `Ciudad de entrega: ${cityLabel}\n`;
        message += `------------------------------------------\n`;
        
        let total = 0;
        cart.forEach((item: any) => {
          const itemTotal = item.price * item.quantity;
          total += itemTotal;
          message += `• ${item.quantity} x ${item.name} (${item.size})\n`;
          message += `  Precio unitario: BS. ${item.price.toFixed(1)}\n`;
          message += `  Subtotal: BS. ${itemTotal.toFixed(1)}\n\n`;
        });

        message += `------------------------------------------\n`;
        message += `Total a pagar: BS. ${total.toFixed(1)}\n\n`;
        message += `¡Muchas gracias!`;
        
        const waNumber = "59179786916";
        const encodedMsg = encodeURIComponent(message);
        const waLink = `https://wa.me/${waNumber}?text=${encodedMsg}`;
        
        cart = [];
        saveCart();
        closeCartDrawer();

        window.open(waLink, '_blank');
      });

      updateCartUI();

      // ── CAMBIO DE COLOR DEL NAVBAR AL HACER SCROLL ──
      const navbar = document.getElementById('main-navbar');
      window.addEventListener('scroll', () => {
        if (window.scrollY > 50) {
          navbar?.classList.add('bg-[#2A1612]/95', 'backdrop-blur-[14px]', 'border-b', 'border-white/10', 'shadow-lg');
          navbar?.classList.remove('bg-transparent', 'border-transparent', 'hover:bg-white/10');
        } else {
          navbar?.classList.remove('bg-[#2A1612]/95', 'backdrop-blur-[14px]', 'border-b', 'border-white/10', 'shadow-lg');
          navbar?.classList.add('bg-transparent', 'border-transparent', 'hover:bg-white/10');
        }
      });
    });
  </script>

  <style>
    @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;1,400&family=Pinyon+Script&display=swap");

    .font-elegante {
      font-family: "Playfair Display", serif;
    }

    .font-cursiva {
      font-family: "Pinyon Script", cursive;
    }
  </style>

  

  <!-- ======================================================== -->
  <!-- COMPONENTES DEL CARRITO DE COMPRA E INTERACTIVOS -->
  <!-- ======================================================== -->

  <!-- BOTÓN FLOTANTE DEL CARRITO -->
  <button
    id="floating-cart-btn"
    class="fixed bottom-6 right-6 z-[990] w-14 h-14 rounded-full bg-[#dcb041] hover:bg-[#FAF5F0] border border-[#dcb041] text-[#2A1612] flex items-center justify-center shadow-[0_4px_25px_rgba(220,176,65,0.45)] hover:scale-105 active:scale-95 transition-all duration-300 cursor-pointer"
  >
    <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" />
    </svg>
    <span
      id="cart-count-badge"
      class="absolute -top-1 -right-1 bg-[#e53e3e] text-white text-[10px] font-extrabold w-5 h-5 rounded-full flex items-center justify-center border border-white"
    >
      0
    </span>
  </button>

  <!-- OVERLAY Y MODAL DE SELECCIÓN DE CANTIDAD (POP-UP) -->
  <div
    id="quantity-modal-overlay"
    class="fixed inset-0 bg-black/70 z-[1100] hidden backdrop-blur-[2px] transition-all duration-300"
  ></div>
  <div
    id="quantity-modal"
    class="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-md bg-[#FAF5F0] border border-[#e8dccb] rounded-[2.5rem] shadow-2xl p-8 z-[1101] hidden flex flex-col items-center justify-between transition-all duration-300 scale-95 opacity-0"
  >
    <!-- Botón de Cerrar Modal -->
    <button
      id="quantity-modal-close"
      class="absolute top-4 right-4 w-8 h-8 rounded-full flex items-center justify-center bg-gray-200/50 hover:bg-gray-200 text-gray-700 transition-colors duration-200 cursor-pointer font-bold"
    >
      ✕
    </button>

    <div class="w-full text-center">
      <h3 id="modal-product-name" class="text-xl font-bold font-serif text-[#2A1612] leading-tight"></h3>
      <span id="modal-product-size" class="block text-xs text-[#8f6c49] font-sans mt-0.5 font-bold"></span>
      
      <div class="w-full flex justify-center my-6">
        <img 
          id="modal-product-img" 
          src="" 
          alt="Producto" 
          class="w-40 h-40 object-contain drop-shadow-xl"
        />
      </div>

      <!-- Selector de cantidad -->
      <div class="flex items-center justify-center gap-6 mt-4">
        <button
          id="modal-qty-minus"
          class="w-10 h-10 rounded-full border border-[#2A1612]/30 flex items-center justify-center font-bold text-lg text-[#2A1612] bg-white/60 hover:bg-white active:scale-95 transition-all cursor-pointer"
        >
          -
        </button>
        <span id="modal-qty-val" class="w-12 text-center text-xl font-black text-[#2A1612]">1</span>
        <button
          id="modal-qty-plus"
          class="w-10 h-10 rounded-full border border-[#2A1612]/30 flex items-center justify-center font-bold text-lg text-[#2A1612] bg-white/60 hover:bg-white active:scale-95 transition-all cursor-pointer"
        >
          +
        </button>
      </div>


      <!-- Botón de agregar -->
      <button
        id="modal-add-btn"
        class="w-full mt-6 py-4 rounded-full bg-[#dcb041] hover:bg-[#2A1612] hover:text-[#dcb041] text-[#2A1612] font-black text-sm shadow-md hover:shadow-lg transition-all duration-300 cursor-pointer"
      >
        Agregar al Carrito
      </button>
    </div>
  </div>

  <!-- OVERLAY Y DRAWER DE CARRITO (SIDEBAR) -->
  <div
    id="cart-overlay"
    class="fixed inset-0 bg-black/60 z-[1000] hidden backdrop-blur-[2px] transition-all duration-300"
  ></div>
  <div
    id="cart-drawer"
    class="fixed top-0 right-0 h-full w-full sm:w-[420px] bg-[#2A1612]/98 border-l border-white/10 shadow-2xl z-[1001] translate-x-full transition-transform duration-500 ease-in-out flex flex-col"
  >
    <!-- Cabecera del Carrito -->
    <div class="p-6 border-b border-white/10 flex items-center justify-between">
      <div class="flex items-center gap-2">
        <svg class="w-6 h-6 text-[#f6d98d]" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z" />
        </svg>
        <h3 class="text-xl font-serif text-[#f6d98d] font-bold">Tu Pedido</h3>
      </div>
      <button
        id="cart-drawer-close"
        class="w-8 h-8 rounded-full flex items-center justify-center bg-white/10 hover:bg-white/20 text-white transition-colors duration-200 cursor-pointer font-bold"
      >
        ✕
      </button>
    </div>

    <!-- Lista de Productos en el Carrito (Scrollable) -->
    <div id="cart-items-list" class="flex-grow overflow-y-auto p-6 space-y-4">
      <!-- Los productos se inyectarán dinámicamente aquí -->
      <div id="cart-empty-msg" class="h-full flex flex-col items-center justify-center text-center text-white/50">
        <svg class="w-12 h-12 mb-3 text-white/20" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 3h1.386l.542 7.962A2.25 2.25 0 006.402 13h10.196a2.25 2.25 0 002.21-1.944L18.75 3m-16.5 0h13.5m-16.5 0l.344-1.5H19.5" />
        </svg>
        <p class="text-sm font-bold">Tu carrito está vacío</p>
        <span class="text-xs mt-1">Explora nuestras colecciones y agrega tus chocolates favoritos</span>
      </div>
    </div>

    <!-- Resumen y Checkout del Carrito -->
    <div class="p-6 border-t border-white/10 bg-[#251210]/60">
      <div class="flex items-center justify-between mb-4">
        <span class="text-white/60 text-sm font-semibold">Total estimado:</span>
        <span id="cart-total-price" class="text-2xl font-serif font-black text-[#f6d98d]">BS. 0</span>
      </div>
      
      <button
        id="cart-checkout-btn"
        class="group w-full py-4 rounded-full bg-gradient-to-r from-[#dcb041] to-[#f4c856] text-[#2A1612] font-black text-sm shadow-lg hover:shadow-[0_4px_25px_rgba(220,176,65,0.45)] hover:scale-[1.02] active:scale-95 transition-all duration-300 cursor-pointer flex items-center justify-center gap-2"
      >
        <span>Finalizar Pedido por WhatsApp</span>
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
          <path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946C.06 5.348 5.397.01 12.008.01c3.202.001 6.212 1.246 8.477 3.514 2.266 2.268 3.507 5.28 3.505 8.484-.004 6.657-5.34 11.997-11.953 11.997-2.005-.001-3.973-.502-5.73-1.45L0 24zm6.59-4.846c1.6.95 3.188 1.449 4.825 1.451 5.436 0 9.86-4.42 9.864-9.864.002-2.637-1.03-5.115-2.905-6.99-1.876-1.875-4.355-2.905-6.997-2.907-5.443 0-9.867 4.42-9.87 9.867-.001 1.698.443 3.355 1.287 4.8l-.996 3.636 3.737-.98c1.413.773 2.997 1.183 4.545 1.183z" />
        </svg>
      </button>
      <span class="block text-center text-[10px] text-white/40 mt-3 font-sans">El pedido se enviará pre-formateado directo al WhatsApp oficial de nuestra fábrica.</span>
    </div>
  </div>

  <CallToAction />
  <Testimonials />
  <Footer />
  <MobileCTA />
</Layout>
